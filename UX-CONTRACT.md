# Mangrove 跨页面交互契约

> 状态：2026-09-04 整改目标草案；不是现有前端合规声明。UX01 原型确认，后续实现逐片核验。

## 产品与业务依据

服务中文业务用户，通过自然语言处理文件、互联网数据与二者混合任务。简体中文、用户可理解的
时间与时区；日期值不能擅自作 UTC 转换。目标 WCAG 2.2 AA。具体视觉方案见
`docs/plans/2026-09-04-unified-workspace-ux-blueprint.md`；当前 Token 实现仍见 `DESIGN.md`。

| 业务域 | 权威来源 | 对界面的约束 |
|---|---|---|
| 统一任务、模型与交付 | CONTEXT.md；ADR-0035/0036 | 一套入口与状态；不得把 Candidate 显示成正式交付 |
| Owner、角色、正文审计 | AGENTS.md §5；ADR-0037 | 隐藏未获准入口并保留后端拒绝；拒绝页不泄漏正文 |
| 外部只读、来源、保留和删除 | ADR-0039 | UI 使用获准动作，删除展示服务端真实引用与后果 |
| 用量和参考费用 | 总整改计划 §3 | 原生用量、未知值、按模型/连接汇总；非支付或收费功能 |
| 法律/平台限制 | SECURITY.md、THIRD_PARTY_NOTICES.md | 不自行增加法律承诺、授权范围或平台写能力 |

## 规范所有者（需在原型/实现核实，不虚构已存在组件）

| 能力 | 当前可复用所有者 | 本轮目标与验证 |
|---|---|---|
| 导航、路由 | React Router、Layout、App | 一套任务入口；历史/设置可回到原任务；浏览器前后退 E2E |
| Button / Input | components/ui/button.tsx、input.tsx | 保留语义、聚焦、busy 几何；不复制页面专用实现 |
| Form | 既有字段 + API 校验；尚无完整共享 Field | UX01 定归属，按需最小共享；错误关联与首错聚焦 |
| Select/Listbox | 现有原生 select；没有强行宣称已有 Radix Select | 原生为基线；需自定义弹层先评估已安装能力/授权安装 |
| Date | 现有原生 date/datetime-local | 操作系统弹层可以保留，显示/时区与中文行为须实测 |
| 表格 | TanStack Table/Virtual + ResultPreview | 只读语义 table，分页/过滤和数据身份一致；不加无需求批量编辑 |
| 分割预览 | react-resizable-panels + 现有预览组件 | 拖动和键盘均可调，窄屏切视图；不强制常驻第三列 |
| Dialog / 确认 | Radix Dialog/AlertDialog、ui/modal.tsx | 统一归属与焦点；破坏性确认用真实动作词，禁止原生 confirm |
| 工作记录 | 已有 WorkTrace 投影 + Radix Collapsible | 默认收起，当前待办独立可见，真实动作且不泄密 |
| Toast | Sonner | 去重且可读屏；重要错误保留在所属页面，不能只闪 Toast |
| Scrollbar / Theme | index.css HSL 变量 + Tailwind | 全局标准属性与兼容回退；明暗/高对比实测，不隐藏滚动条 |

## 同类行为与导航结果

| 操作 | 等待与成功 | 失败与焦点 |
|---|---|---|
| 提交需求 | 当前任务显示原始要求及真实请求状态；明确进入澄清或执行 | 保留文字/已上传资料；失败不伪造 Run，回输入或待办 |
| 上传 | 每文件进度/验证/重试/取消；成功可预览 | 单项失败不清空其他文件，错误紧邻文件；文件选择按钮可键盘使用 |
| 修改要求 | 产生真实新修订，当前视图明确版本；旧结果可重开 | 冲突先重取状态，不能覆盖新事实；焦点留在输入 |
| 查看结果 | 结果、来源、QA、版本和下载身份一致 | 切换时不暂借另一版本正文，说明加载/失败；关闭画布返回触发处 |
| 停止 | 先显示停止请求，服务端确认静默/清理后显示已停止 | 未知或清理失败继续展示待处理；不因断开浏览器 SSE 就假成功 |
| 登录待办 | 区分平台账号登录与来源扫码；完成后回同任务并重新验证状态 | 失效/网络未知分别提示，不重复执行未知结果调用 |
| 复用数据 | 选择本人资料/结果后回当前输入；标明原始/派生与获取时间 | 失权/已删除重新校验，不能回退缓存内容 |
| 删除任务 | 使用 ADR-0039 的引用分支，完成后返回列表并修正分页 | 保留确认范围和失败原因，不乐观假删，不承诺不存在的撤销功能 |
| 模板/记忆编辑 | 成功返回所属列表或调用它的当前任务，保留上下文 | 保存失败保留表单；离开未保存编辑用应用内确认 |

## 状态、数据导航与恢复

- 会话草稿、前置来源获取和执行 Run 身份分开，但同一任务连续呈现；任何时间/Token 汇总不重复。
- 搜索任务历史应有清除按钮和 IME 安全防抖；过期请求取消或忽略。后台分页/排序作用于完整获准
  数据集，不能把当前页过滤伪装为全量。只有非敏感导航状态可进入 URL；业务 Query、文件正文、
  令牌和凭据不得进入 URL。不新建隐私数据的浏览器持久副本。
- 服务端是状态权威。删除、登录恢复、权限、提交、交付等不作无依据乐观更新；重试先查询结果，
  具有幂等身份，处理完成未知。切任务/修订时取消旧订阅，缓存键绑定 Owner 与事实身份。
- 流式处理保留跨块 UTF-8/SSE 缓冲；去重并按持久事件顺序投影。用户上翻后不抢滚动，提供回到
  最新入口。状态适量读屏播报，不逐 token 宣读；安全 Markdown，不能执行来源 HTML/脚本。
- Enter 提交与 Shift+Enter 换行支持中文组合输入；输入可自增高和展开，控制可见且不被软键盘遮住。
- 待用户确认/扫码不藏在工作记录里；部分结果和缺口同时可见，不伪称全网无数据或任务全成功。
- 断网、超时、过期会话、权限变化、服务重启、并发修订必须有明确恢复路径；不能用无限 Spinner。

## 响应式、权限与反馈

- 任务导航只保留一层常驻所有者；窄屏为可访问抽屉。对话/来源/结果切视图保持同一任务身份。
- 各区域明确滚动归属；宽表内部滚动不能截断同层长文/表单。专注结果返回保持原位置。
- 路由有中文标题；404、403、会话失效和服务异常分别解释，并保留安全导航；不一律跳回概览。
- 可执行操作用 button，导航用 link；禁用有可访问原因。权限隐藏只改善体验，后端仍独立校验。
- Dialog 有可访问标题/说明、焦点约束和恢复；高风险确认先聚焦安全按钮。浮层堆叠在共享层实现，
  不让每页面追加任意 z-index。Secret 遮罩，禁止出现在 Toast/URL/浏览器日志/导出中。
- 只读 API、导出、预览、文件链接、模板和记忆都服从相同 Owner/内容审计边界；不因是管理页放宽。

## 迁移与验证

### 运营与审计（2026-09-17）

- `/operations` 为管理入口；管理员查看普通用户和本人，超管全平台，普通用户拒绝页不暴露数据。
- 五个子页共用北京时间范围，图表和指标下钻保留筛选；完整时间线包含登录、访问和操作。
- 日志及时间线服务端分页，每页 10/20/50 条；用户汇总按已返回的前 100 位分页并标明范围。
  筛选或条数改变回第一页，越界页修正到有效页；翻页显示新页顶部，详情关闭保留列表上下文。
  排行、趋势时间点与常用视图亦限制每页数量；页面末端不允许继续拖动外层框架产生空白。
- 登录未知账号不可打开用户时间线；请求成功与异步任务完成分开解释。
- 查询和详情有超时、失败及原位重试；自动刷新可选且只在前台运行。过期角色请求不更新界面。
- CSV/XLSX 导出经过确认并限制条数；个人视图可保存和删除，不删除日志。仅超管能确认保留策略变更。
- 前台心跳仅估计在线及活动时长；采集前数据和保留范围不足的同期比较不补造为零。

路线和能力矩阵归 GitHub 整改父票，UX01 冻结；每片核对旧入口、目标入口、证据、移除条件。
不做历史任务迁移；替代能力未通过不能删除唯一实现。最后关闭旧入口的新任务接收并清理已失效
代码/文案，保留正式历史证据和真实用户数据。

既有 `npm run build`、`npm run test:e2e`、axe、Playwright 和后端定向回归按切片风险执行。
代表矩阵：1440px、390px、短视口、200% 缩放、明暗主题、中文 IME、键盘与 reduced-motion。
同类行为至少比较一个兄弟页面。每项应有成功、错误、取消和适用恢复证据，不以静态审计替代。
上段规划状态不能代替各切片最新验证；具体工程证据以docs/status/current.md为准，测试通过不代表用户验收或产品发布。

### 记忆管理（2026-09-19）

- 我的记忆与全局记忆在内容区切换，输入入口与作用范围同区，不新增页眉。工作台管理弹窗复用个人记忆交互。
- 记忆页未保存输入或保存处理中，浏览器前进/后退先确认；取消保留草稿及历史位置，确认后重放原跳转，不新增历史条目。跨文档离开保留浏览器原生卸载提示。
- 个人编辑原位展开，搜索及翻页保留未保存编辑；保存时按当前筛选回读，未显示的新增记忆提供定位入口。默认10条，支持20/50/100；空集合不展示无效分页。
- 删除先确认；全局整篇保存确认作用范围，冲突保留输入。新增草稿与整篇编辑互不清空。搜索中文组合输入结束后防抖查询，读取失败保留已有内容。
- Markdown使用安全资源模式，不自动加载外链图片；输入区增高上限240px，超出内部滚动。明暗主题和窄屏沿用现有tokens。

### 反馈管理（2026-09-20）

- 统计卡采用全平台累计任务/点赞/点踩/待处理，点踩率以任务数为分母；页面筛选只影响明细及导出，零任务不展示虚假比率。默认10条，支持20/50/100；搜索兼容中文组合输入，日期无效不查询，失败保留上次内容。
- 查看个人业务内容仍先填写审计原因；详情采用可访问侧栏，操作区固定、正文独立滚动，关闭清理私人内容。回答可安全Markdown渲染，原始文本/JSON预览必须保留原字符。
- 列表不提供快捷处理/忽略；详情审计核对后填写结论，选择已修复/无需修改/暂不处理并提交。旧已处理/已忽略保持原义，不自动映射已修复；新处理结果必须始终保留非空结论。
- 保存备注后保留详情、不改变状态；保存与结果变更可一次提交，处理中防重并重新读取已审计内容。未存备注或结果选择关闭/浏览器后退需确认，取消保留编辑；删除失败不关闭确认或自动重试。
- 工作台实时连接中断不等于任务失败或成功；有可信任务身份则只读恢复进度，未知结果明确提示勿重复提交，不自动重跑。
