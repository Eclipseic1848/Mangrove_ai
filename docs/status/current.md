# Mangrove 当前状态台账

## 公开代码同步边界（2026-09-20）

- 用户已授权将本机最新源码、测试及相关公共文档同步 GitHub。本轮目标为现有开发分支 `codex/issue-140-universal-context`，不是直接更新 `main`、创建版本或发布生产环境。
- 合并阻塞：本地与远端 `main`（`ec4cf059`）的 `scheduler_0002/0003`、`webui_0019/0020/0021` 存在同号不同内容；两条迁移链不能直接混合。后续合并前须设计兼容迁移并用两种历史数据库副本验证，不能改写已执行 revision 或覆盖原库。
- README 已同步自动化、记忆、经验库、运营 Token、反馈与引导，以及初稿等待用户决定的两条发布路径；ADR 索引补齐 0039–0047。Contributing、行为准则、MIT License、Security 和 GitHub About 已检查，现有语义无需变化。
- 本机审计文件、独立演示原型、冻结评测 WIP、品牌图片和运行数据不公开。概览图片为可选资源，缺失时使用通用图标；不改变本机已有图片效果。此同步不代表整体 P1、真实用户验收或生产门完成。
- 发布检查修正：Windows 导出未指定换行属性的旧迁移时可能转成 CRLF，与冻结 LF 摘要不符；统一迁移 Python/SQL 的 Git 属性为 LF，未修改历史迁移内容或冻结摘要。重新导出的暂存树已通过完整 revision manifest 校验。概览浏览器用例兼容无品牌图片，并独立跳过教程以验证业务操作；教程仍由独立 34 项覆盖。
- 开发分支源码提交 `8120180` 已推送且核实远端一致；README 等内容已在该分支更新，main 未改变。当前分支可达历史 Secret 扫描通过。GitHub 当次回执提示默认分支 22 项依赖告警（2 critical、8 high、12 moderate），仍需独立核实与修复，不属于稳定发布结论。
- 收尾隔离源码副本验证：迁移、运营用量/审计、记忆管理、反馈与自动化108项通过；修正最新迁移头0022断言及合成历史空库夹具，不改变生产迁移。另有记忆/初稿37项组合、引导34项、概览9项通过；不代表全仓回归或用户验收。当前分支推送未触发新的远端CI。

## 按角色的新手引导（2026-09-20，已本机更新，待用户验收）

- 验收修正：反馈教程在整屏高亮目标处曾上溢261px；共用定位启用两轴视口避让，滚动和窗口缩小时保持气泡可见。设置页教程与刷新归入右侧操作组；移除工作台旧帮助入口及独占弹窗/状态，保留新手教程和四分类任务示例。类型/构建通过，34项完整引导、2项工作台主题回归及8088五项定向检查通过；覆盖390/1440/2554px及缩小至600px高度，实际截图已核对。仅前端更新，无后端重启或真实业务操作。
- 概览、任务工作台新建与已有任务、自动化计划与记录、模板/教训/巡检、个人/全局记忆、设置各分区、运营各子页、反馈、用户管理及历史对话接入独立引导。只展示当前角色可访问的页面与步骤，不扩大后端权限，不自动操作业务按钮。范围及存储约定见 `docs/plans/2026-09-20-role-onboarding.md`。
- 首次就绪后自动播放；完成、跳过本页或全部后不再自动打扰，页面操作区始终保留“新手教程”。按用户、角色和子页保存在本机同源浏览器，清理存储或换设备会重新引导；存储不可用时提示并退回当前页面会话。
- 复用已安装的 React Joyride；遮罩、目标交互阻断、焦点陷阱、Esc/方向键、减少动画、窄屏与深浅主题已接入。输入时延后，失焦后重试；给已有或新出现的业务弹窗让位。等待异步数据，Token首次失败不误记完整教程已完成，恢复后再播放。
- 类型检查、构建通过，构建保留现有大包警告；开发29项及新增失败恢复/已有任务2项通过，8088实际构建31项隔离API浏览器回归通过。桌面/390px深色截图已检查，教程气泡自动无障碍检查通过；已有运行任务完整引导未发送追问或取消请求。双轴审查发现项已闭合。以上为工程验证，不代表真实用户验收或全站业务回归。
- 仅更新前端静态入口并保留旧资源；无后端重启、数据库迁移、真实任务/账号操作、Git提交或推送。主要功能入口已覆盖，不自动打开编辑/处理弹窗执行教程操作。

## 初稿由用户决定后续核对与图标操作（2026-09-20，已本机更新，待用户验收）

- 工作台完整初稿冻结后结束当前执行并等待所有者决定，不再默认进入后续核对。用户可接受初稿，或明确继续同一执行会话的核对；接受仍保留用户采纳语义，不冒充系统验证通过。缺失原会话时拒绝自动创建新运行。详见ADR0047，取代此前默认自动核对策略。
- 初稿完成信号、模型结束和已有冻结记录三种入口均受暂停门保护；占位文件不提前交稿，撤权异常不吞掉。继续接口在事务中绑定Owner、版本、初稿与Run，防重复提交，刷新和重启不自动批准。
- 发送/停止采用40px圆形箭头/方块纯图标，保留可访问名称、悬停提示、处理中禁用和取消确认。追问输入与停止共用入口的既有行为不变。
- 追问链路58项后端回归通过；可保存和恢复历史、查询进度、确认修改后建立新版本并维持用户隔离。文件任务追问只携带冻结目标、状态、近期事件、来源轻量信息及相关业务修改回合，不自动读取完整结果或全部聊天历史；尚未验证真实模型多轮问答质量。
- 初稿接口26项通过；运行时69项回归通过、1项真实Docker门跳过，隔离副本缺少dockerignore的单项补齐后通过；集成后关键暂停/恢复6项通过。类型/构建通过，8088实际构建隔离API页面9项通过，桌面与390px图标截图已核对；双轴审查发现项均已关闭。
- 真实Pi/DeepSeek合成PDF验证确认初稿暂停、会话保留、明确批准后同Run恢复首条模型响应及资源清理；探针随后主动结束，不能据此宣称完整后续核对端到端通过。未重跑用户原任务。
- 按授权完成源码、两库及旧入口备份，无运行任务/验证租约且巡检关闭后安全集成并重启后端；8088健康正常，任务33条、修订36条、计划10条、运行111条与备份逐行一致，两巡检保持关闭。无Schema迁移、Git提交或推送；已发出的上游请求可能仍产生费用。

## 工作台统一发送与停止入口（2026-09-20，已本机更新，待用户验收）

- 对话采集的停止等待/停止执行移至输入框发送位置，取消在途禁用并绑定发起时连接；文件任务取消确认与重试停止通过现有Portal模式移至追问输入框，未输入追问时与发送互斥，输入追问后保留发送能力。不修改后端取消语义。
- 类型/构建通过；开发和8088实际静态资源各5项隔离API验证通过，覆盖互斥、追问、取消确认退出、取消后恢复、失败重试、连接恢复和网页刷新停止。390px截图已检查。未重启、未操作真实任务、未提交推送。

## 文档范围规划修复（2026-09-20，已热加载，待用户验收）

- 文档工具及提示词区分检索边界与分批读取。低置信度的单元限制在冻结前拒绝，允许重新规划，不改写历史契约；范围拒绝不再误报解析故障。
- 运行时及中继91项通过、1项真实Docker门跳过；补充冻结前拦截后中继20项通过。真实DeepSeek合成12页规划探针的正常规划、错误范围被拒后重新规划均通过。
- 经用户授权，校验器相关5文件完成双轴复核并本地提交9eba189（未推送）；LessonAssessment纳入身份闭包，规则允许列表升为v2，未放宽校验门。提交后44项测试通过。
- 合成7页PDF的真实Pi/DeepSeek执行完成第5张审批单提取、JSON内容检查及独立文件/来源证据/目标语义校验，状态PASSED。此为隔离数据工程验证，不代表已重跑用户原PDF或完成用户验收。
- 已核实本机自动热更新在源文件编辑后重载了后端，更正此前“未上线”表述。本轮未额外重启、未重跑用户任务。反馈点踩专属处理页面已更新8088，10项实际资源模拟接口UI、36项后端回归通过。

## 工作台用量入口（2026-09-20，已本机更新，待用户验收）

- “查看本次用量”增加图标、主题色浅底/描边、加粗文字、40px点击区域、方向提示和键盘焦点反馈；用量统计、版本关联与弹窗内容不变。
- 类型/构建通过；8088隔离页面三项定向验证通过，覆盖深浅主题、390/1440宽度、键盘打开/关闭后焦点返回及历史用量刷新。无后端重启或真实任务操作。

## 用户管理体验（2026-09-20，已本机更新，待用户验收）

- 修改昵称、重置密码、创建和删除失败保留弹窗及输入，提交防重复；业务请求超时按未知结果处理，不自动重发。确认放弃创建草稿会清空用户名、密码及角色。
- 角色调整、启停账号增加确认及影响说明；“拒绝并删除申请”保留既有删除语义和后端分级权限。保留账号后台处理、失败重试、迟到回执与轮询安全边界。
- 紧凑注册配置明确已开放/已关闭/读取失败，可重试；新增用户详情、文字操作、表单标签及密码显隐。分页复用既有组件，10/20/50/100默认10，筛选复位、末页回退、空态保留控件；窄屏、深浅色及中文输入优化。
- 开发回归34项、类型/构建通过；双轴复核问题已修。8088实际构建隔离模拟API34项通过，覆盖分页、错误恢复、超时、权限呈现和既有账号治理竞态；桌面1440与窄屏390截图、主区与弹窗自动可访问性检查通过。仅前端更新，无后端重启、Schema迁移或真实账号写入，未进行真实账号业务验收。

## 反馈管理体验与任务口径统计（2026-09-20，已本机更新，待用户验收）

- 处理流程已进一步修正（替代下文的“标记已处理”快捷按钮）：列表不再提供处理/忽略操作；管理员先审计核对详情，再填写结论并选择“已修复/无需修改/暂不处理”。旧已处理/已忽略记录保持原值，不自动宣称修复。后端在事务中核验本人对当前正文的审计证据与非空结论，三类新结果提交后不能清空结论；仅保存备注不改变状态。保存后重新审计读取当前备注，支持继续处理。现有运营审计保留处理人、时间、对象与状态变化；无Schema迁移。
- 本轮34项后端分组回归、34项页面分组回归及类型/构建通过；Spec复核指出的清空结论漏洞已补接口和页面测试，Standards/Spec最终无剩余必修。备份、无运行任务/租约、巡检关闭门通过后本机更新，8088实际构建隔离模拟API34项全过；健康正常，真实反馈/任务/记忆/用量/计划与备份一致。未真实处理用户反馈，待用户验收。

- 验收微调：移除点踩率卡片下方公式说明；列表动作改为“标记已处理”，已处理记录仅保留状态徽标、不重复显示处理按钮。仅前端更新，计算及状态接口不变；上线模拟页面定向3项通过，无后端重启。

- 首卡改为平台总任务数；点踩率为全平台点踩累计除以全平台去重任务数。工作台、数据处理及有明确任务ID的旧对话按用户/任务身份合并，版本、重试和重复消息不重复计数；包含留存的全部任务状态，空会话不算任务，已物理删除且无记录的历史无法补算。零分母显示“—”。统计卡为全平台累计，筛选仅影响明细与导出。
- 首屏压缩统计与原因区域；列表默认10条，支持20/50/100，搜索防抖并兼容中文组合输入，日期倒置不请求，读取失败保留已有列表。详情采用可访问侧栏、固定操作区和安全 Markdown 回答；原任务文件文本保留原字符，预览定位、失败重试分页位置、工作簿行列定位及 PDF 文字选择同步优化。
- 保存备注不关闭详情，可一次保存并处理/忽略；未保存备注关闭、浏览器后退有确认，保存期间防重；删除失败保留确认和错误。Owner、角色、版本冻结、原因审计、晚到响应及正文不持久缓存边界不变。
- 工作台实时连接中断但登录仍有效时不再显示红色失败提示；按已有任务身份只读恢复进度，未知结果明确待确认，不自动重新提交任务。
- 隔离后端33项、前端53项（末页时序修正后定向通过）及工作台连接恢复1项、类型检查与构建通过；两轴审查复核无剩余必修。按授权备份两库和原构建，确认无执行任务/验证租约且巡检关闭后更新；8088实际构建模拟API复验33项反馈与1项连接恢复全部通过，深色可访问性及390/1440布局已核对。首次启动等待期间连接拒绝的失败证据保留；健康就绪后重新通过。无DDL、真实模型/邮件调用、任务重跑或Git提交推送，关键业务表与备份一致；工程验证不等于用户验收。

## 反馈原任务追溯（2026-09-20，已本机更新，待用户验收）

- 点赞／点踩详情新增“查看原任务”：读取被评价版本的原始需求、相关问答、输入资料和全部已登记结果文件，不以执行摘要替代实际结果，不重新执行任务。旧会话从被评价消息的持久附件关联恢复文件；缺失关联明确说明，不猜测或补造。
- 原任务对话分页、长正文分段；文本、工作簿窗口与 PDF／Office 预览复用现有能力，完整原件可下载。文本预览8MB、Office20MB、单消息2097152字符上限；Office需既有隔离转换服务，超限或格式不支持时提示下载原件。未新增依赖或迁移。
- 父审计原因绑定管理员、反馈、Owner、版本和结果，30分钟有效；后续查看／预览／下载分别留痕，审计失败不返回正文。正式结果与输入继续核验冻结身份及完整性，单项输入失效不遮蔽其他输出；关闭、账号变化和晚到响应清除私人正文。
- 隔离后端46项、页面25项及类型／构建通过，Standards／Spec修正复核无剩余必修项。按授权备份并确认无运行任务、巡检关闭后更新本机；健康正常，关键业务表与备份一致。上线静态资源模拟API复验25项全部通过；工程检查不等于真实用户验收。

## 反馈管理修复（2026-09-20，已本机更新，待用户验收）

- 上线后工作台历史对话、草稿和文件结果三种操作栏追加复验3项通过；合计25项实际静态资源模拟API页面测试通过。真实反馈及任务等关键表再次与备份比对一致，健康正常。

- 点赞与点踩统一提供“查看反馈详情”：管理员填写原因后，查看原始任务、当前提问、对应回答、用户说明及处理备注；跨 Owner 正文仍经显式审计，不进入列表或 CSV。正式交付补充首个文本产物预览，读取前核验 Owner、版本、QA、清理状态和文件完整性；文件上限8MB、正文最多512KB，办公文档不在审计事务中解包，界面明确限制。
- 修复用户名搜索、北京时间日期筛选、反馈口径点踩率、末页删除回退、读取失败误作空态；修改评价重新待处理，完全相同重试不覆盖处理状态。处理与删除留痕明确反馈对象和状态差异；超长输入拒绝，超过10000条导出明确要求缩小范围，不静默截断。
- 最终隔离后端35项、管理页面22项、TypeScript和构建通过；两路审查的正式结果缺失及预览资源边界问题均修正复核。按授权备份源码及两库、确认运行任务为零且巡检关闭后集成重启，健康正常；8088实际静态资源上22项模拟API页面复验通过。无DDL、不改写历史反馈，不执行真实模型或邮件；工程验证不等于真实用户验收。

## 运营审计页眉与用量统计（2026-09-19）

- 中国区报价上线复验完成：健康检查及3项模拟页面测试通过，真实任务、修订、用量、记忆及定时记录与上线前备份一致；无数据库迁移或真实模型调用。待用户验收，不代表实际账单完全一致。

- 2026-09-20中国区报价已更新：官网人民币原价覆盖DeepSeek2、Qwen北京2、GLM3共7款；人民币不再从美元报价换算，美元仅为展示折算。Qwen北京业务空间入口可匹配，同型号国际地域不套价；Kimi现有国际端点不套用已查得的中国价。无适用中国区报价仍未估价，本地仍不计价。27项隔离接口与3项模拟页面、双轴复核通过；本机集成后待上线复验。缓存/峰谷折扣和附加费用仍不在基础参考估算内。

- 计价修正上线复验已完成：健康检查正常，8088静态资源上两项模拟API页面测试通过；运行任务0、两巡检仍关闭。待用户验收，不代表所有支持型号都已有报价。

- 2026-09-20计价范围修正：价格展示与计算限定平台模型目录，移除目录外旧型号默认价格；当前两款DeepSeek具备已核实默认报价，其余平台型号明确显示未配置报价。历史用量不删除。本地/托管私有模型只统计Token和请求，显示“无需计价”，云端估价覆盖率排除本地请求；混用及导出同步。隔离后端25项、页面2项、类型/构建及双轴复核通过，按安全流程已集成重启，待上线复验及用户验收。

- 管理导航顺序已更新为运营审计、反馈管理、用户管理、设置；角色可见范围不变。三角色桌面/抽屉及主题/草稿/退出模拟回归5项通过，类型及构建通过，前端增量更新后5项模拟API复验通过；未重启后端。

- 页眉已与记忆等模块统一：标题18px、说明14px、上下16px及左右28px内边距；正文独立滚动，页眉保持固定。仅修改运营页面样式和页眉结构，未调整业务数据或权限。
- 页面回归19项通过，类型检查与构建通过；本机前端增量更新后，模拟API复验页眉和底部滚动2项通过。已检查390px深色截图；未重启后端，旧静态资源保留。工程检查不等于真实用户验收。
- 分用户Token统计已完成接口、页面及可配置价格，本机已更新，待用户验收。仅聚合可信调用记录，不回填未知历史；支持用户/模型/接口筛选、币种、排序、分页、模型明细和全筛选范围CSV/XLSX导出。普通用户拒绝访问，管理员范围复用现有规则，查看/导出留痕。无数据库迁移；未知用量和未匹配价格不当成零。汇率7仅示例假设，非实时汇率。配置与验证见 `docs/plans/2026-09-19-token-usage.md`。
- 隔离后端组合23项、前端组合20项及类型/构建通过；双轴审查问题修正后复核通过。按用户授权，备份后确认无运行任务、两巡检关闭，安全重启后健康检查正常。工程测试不等于真实供应商账单核验。
- 上线静态资源模拟API复验25项全部通过，覆盖Token、原运营页面及三角色导航；首轮早于服务就绪的连接失败已保留并完成重验，未读取真实日志进行页面测试。

## 记忆前端体验优化（2026-09-19，已本机更新，待用户验收）

- 内容区以“我的记忆／全局记忆”切换，范围与入口对应；共享个人管理组件统一工作台弹窗与记忆页面。原位编辑、筛选翻页保留编辑、筛选下新增定位、删除确认、中文组合输入防抖、刷新失败保留正文、全局安全预览及保存确认均已覆盖。
- 隔离模拟API页面29项通过，覆盖工作台入口、三角色、明暗主题、390/1440宽度和axe；类型检查、构建及定向双轴复核通过，构建仍有既有大包告警。增量更新8088前端后，模拟API复验15项全过；未改后端、真实记忆、数据库或模型调用，未重启服务。
- 草稿保护覆盖范围切换、搜索翻页、站内链接离开与页面卸载提示；后续补丁已补齐浏览器前进/后退及多步历史导航：取消保留草稿，确认重放原历史跳转，同路径历史也重建草稿，保存处理中阻止离开。最终上线资源模拟API复验18项通过，类型/构建及双轴修复复核通过，未重启后端。上述工程证据不等于真实用户验收或整个记忆路线完成。

## 记忆管理与工作台自动参考修复（2026-09-19，已本机上线，待用户验收）

- 对话补漏已本机上线：新建工作台对话注入本人及共享偏好，短追问结合近期用户原话保持相关性；已有任务追问只参考该版本冻结记忆。独立发送“记住：完整偏好”或“忘记：完整记忆”可持久保存或本人精确软删，不调模型、不触发邮件、不改历史任务；否定、问句、引用不作为修改授权，重放失败关闭。零模型调用明确显示0，缺失用量仍保持未知。
- 本片相关后端32项与追问27项通过，最终零用量标记再跑11项通过；页面9项、类型/构建、Standards/Spec复核通过。隔离数据库中真实调用本机Qwen3.6-35B-A3B，验证对话保存后新会话召回、当前要求优先、跨用户隔离、删除后新会话不再召回四项；仅该模型和合成场景的效果证据，不代表所有供应商或任意语言表达均已验收。
- 安全上线前两库备份完整性通过，确认无运行任务、执行锁或验证租约，保持两巡检关闭；按9项源码/测试允许列表集成，重启后8088健康正常。上线资源模拟API复验9项通过，真实个人记忆、任务和版本记录与备份逐行一致，无DDL、真实记忆写入或邮件发送。回退与进程实况见handoff。

- 验收展示补丁：全局添加归入全局卡片，明确共享范围，编辑按钮不再整行拉伸；窄屏换行、普通用户只读。当前全局记忆文案删除旧开发路径/版本说明，改用用户语言，保留原偏好及追加识别小节。三角色等8项页面回归、类型/构建及两轴复核通过，部署静态资源模拟复验8项通过；未重启后端、未执行真实任务或改变权限，回退点见handoff。

- 已修复并集成：个人创建/纠正长度一致、旧超长条目可纠正、去重与搜索分页；平台规范管理员摘要并发编辑；页面加载失败不冒充空态、正文可滚动查看、编辑冲突保留输入、按账号重建页面状态。
- 当前PI工作台按相关性与预算选取个人偏好，平台规范在显式预览阶段纳入并绑定摘要；已创建任务沿用冻结快照。候选不再仅限最新50条，使用单次查询替代逐条开库；旧意图链使用有界脱敏内容。此实现是关键词匹配，不是向量语义检索；明确对话命令已在上述补漏片实现，任意表达的自动提取、TTL及向量索引未引入，未自动清理真实记忆。
- 公开接口已验证写入→文件执行请求→删除/修改→历史快照不变→新网页任务使用新规范。Standards发现读取失败可遗留幂等认领，已把准备前移并对准备/冻结读取异常返回友好503；Spec发现显式预览漏规范，已修复。两轴复核无剩余必修项。
- 最终固定种子组合75项通过；此前扩大组39通过/2等待终态超时的证据保留，两个用例亦独立通过。TypeScript和前端构建通过，保留大包与依赖告警。记忆页面隔离5项、概览/记忆组合13项通过；部署资源再次以模拟API验证5项通过，不等于真实模型效果或用户验收。
- 用户明确批准安全上线：备份两库且完整性通过，通过现有配置接口关闭模板/Cookie巡检并保持关闭。上线前工作台/调度无运行任务、执行锁零占用、验证租约为空；11条旧活动绑定保留原值，不擅自清理。停止经归属核验的后端监督链后按11项清单补丁集成，文本逐项与测试版本一致；无DDL。恢复服务后8088首页及健康正常；个人记忆、工作台任务及修订逐行与备份一致。源码与数据库回退点见handoff，未提交推送。

## 页眉与管理入口一致性（2026-09-19，已更新前端）

- 工作台和概览标题统一18px半粗体与紧字距，说明14px；页眉左右28px、上下16px，对齐任务中心和模板库。模板库管理入口保留筛选区位置，改为现有品牌色实心按钮；任务中心移除指定时区/调度状态提示行，未改变调度时区和执行逻辑。
- 概览与记忆模拟页面组合13项通过，覆盖三角色、390/768/1440、分页、失败恢复及字体间距；概览静态资源先增量部署、保留旧资源且当时后端进程未变。后续记忆安全上线已纳入这些展示调整，详见上节。

## 工作台清理入口横条修复（2026-09-19，已更新前端，待用户验收）

- 未完成清理的手动查询按钮原处于页面根级纵向布局，导致拉伸为标题上方整行。现移入工作台头部按钮组，短标签“清理记录”，完整只读说明保留在提示中；窄屏允许换行。未改清理记录、Owner、幂等或手动恢复确认逻辑。
- 新增布局回归先红后绿；最终17项隔离浏览器检查通过，覆盖390/1440明暗主题、旧记录进入不弹窗、查询零清理写入、跨Owner/标签及未知响应保护。类型检查与构建通过，保留原大包和行尾警告；Standards指出可访问名称不含可见标签，已去掉覆盖名称并复核关闭，Spec无必修项。
- 只增量部署前端静态资源，保留旧8个资源文件并切换HTML入口；8088返回新入口、健康正常，后端PID未变。未重启后端、操作真实任务、调用模型或清理资料。首次跨卷备份的原子替换失败，旧入口保持可用；改同卷原子替换后成功。回退与证据目录见handoff。

## 工作台旧清理自动弹窗修复（2026-09-18，待用户确认）

- 本地保存未完成清理记录时，TaskDeletionDialog 原先将 open 初始化为 true，导致进入工作台即弹旧操作。现默认不弹，保留原 Owner 绑定记录、只读查询和手动查看；未改变恢复确认或后端 runtime_stop_unconfirmed 停止保护，未执行真实清理。技术码改中文说明，明确可关闭并正常使用工作台；不宣称底层运行已停止。
- 实际前端源码和构建产物已更新，不切换学习 stage、不迁移原库、不重启后端。Browser 插件未提供，使用已有 Playwright 本地 4173 模拟接口。新用例先复现被弹窗阻断输入，修复后进入/刷新不弹、手动核对、原键保留、零清理写请求通过；首次新用例把访问埋点 POST 也算清理，已限定清理接口。最终定向 20 passed，包含深浅主题、390/1440 窄宽屏、IME、焦点、可访问性及跨 Owner/标签安全。TypeScript/Vite 构建和差异检查通过，保留大包/行尾告警；Standards/Spec 定点源码复核无必修项。
- 广组 22 项曾有 3 失败：上述测试计数问题已修正；另两项“来源墓碑限制重跑但保留独立正式结果和历史QA”“历史网页删除不能借当前完整版本或旧缓存恢复原文”失败在预览 alert 断言，未确认是否既存，不计通过，不扩大本次修复。浏览器证据保存系统临时目录 mangrove-cleanup-dialog-final；源码回退副本见 handoff。真实用户历史操作状态未查询、未改写。

## 模板、教训与巡检补齐（已本机部署，待用户验收）

- 2026-09-19 用户批准关闭自动巡检并保留关闭后，按明确文件清单集成源码，完成原库备份、webui_0022显式迁移及备份恢复核验。迁移前后104张既有表逐行摘要一致；仅此前另行授权的库巡检开关改False，不改其他配置。源码、旧构建、原库回退点与恢复注意事项见handoff。
- 部署目录回执/巡检/权限59项通过，前端类型检查和构建通过；此前隔离组合143 passed、1 skipped（符号链接权限）及修正后的巡检8项保留为工程证据。未安装依赖，保留已有依赖和大包警告。
- 后端监督服务已恢复：8088首页200并加载新构建，健康接口ok，原库Schema current。自动库巡检保持关闭，须由用户决定何时恢复。未主动运行真实任务、模型、邮件或库巡检，不能称真实模型效果已验收；未提交推送。下列未部署描述是分阶段历史证据，不再代表当前部署状态。

- 切换前安全门：运行源码与回退副本一致，活动工作台任务0；但真实全局配置已开启库巡检，重启会立即扫描并可能真实模型合并/物理清理，超出此前不执行真实巡检范围。未改开关、未停止服务、未迁移或切换。等待部署期间巡检运行边界决定，不能按环境默认关闭误判。

- 原库一致副本迁移演练通过：webui_0021→0022，104 张既有表逐行摘要一致，新回执表为空；幂等重放无新增迁移，备份恢复校验通过。仅操作本机私有临时副本，真实库、运行源码和服务未切换；证据与早期命令失败见 handoff。正式集成部署仍未完成。

- 集成准备：十文件隔离组合 143 passed、1 skipped（Windows 符号链接权限）。Standards 找到巡检替身断言会被业务 catch 吞掉的测试缺陷，移到测试主体后巡检8项复测通过并复核关闭；Spec 无必修项。组合运行先于断言修复，两份证据分开记录。最终前端类型检查与构建通过，仍有大包提示；源码回退副本已逐文件核对，路径与范围见 handoff。尚未集成运行源码、备份迁移原库或部署，不代表真实模型/用户验收。

- 语义巡检续验：workspace_table 在两个库通过真实候选过滤/融合解析/落盘/报告投影链，外部模型、向量排序及日志存储模拟。成功各合并一条，跨 Owner 原件保留；模型失败不删除、不覆盖，报告零合并。巡检文件合计 8 passed、2 warnings。无产品源码改动、未部署；不代表真实语义质量或所有类型逐一验证。新增测试收尾审查与累计集成回归仍待。

- 续验：模板/教训合并落盘的跨 Owner 拒绝、候选正文漂移拒绝及原件保护补测通过，巡检文件合计 6 passed、2 warnings。仅隔离测试源码新增，无产品修改；完整语义选择/模型融合链仍待，不把物理删除策略称作历史来源归档，未集成部署。

- 搜索与巡检子片隔离完成（2026-09-19）：模板/教训支持标题、关键词、正文搜索及任务类型/状态组合筛选，两库独立条件，筛选回首页，只处理已有授权列表。最终模拟页面/共享 11 passed，四类 workspace 草稿过期巡检 4 passed；TypeScript/Python 编译通过，Standards/Spec 无必修项。巡检实际操作临时文件、模拟日志存储，经公开生命周期及报告接口核验删除/保留/权限；关闭语义合并，不代表模型融合验证。390 窄屏截图已复核，隔离服务已停。未部署、未操作真实数据，本片未重跑生产构建。下一步为新经验语义去重/合并边界与最终累计集成、回归及备份迁移门；下条搜索和过期巡检待办由本条更新。

- 三 Tab 展示修复隔离完成（2026-09-19）：窄屏标题与按钮分区；巡检失败明确显示可刷新错误，不冒充空记录；自动学习文案与新工作台口径一致，方法核验次数从 API 白名单投影，不公开来源任务，不改旧均分或未知统计。后端 68 passed、1 skipped（Windows 符号链接权限），最终页面组合 25 passed，类型检查/构建/编译通过；Standards/Spec 与测试补审无必修项。截图复核 390/1440 明暗布局；旧文件手动配置测试改测实际 mixed 入口，另保留纯文件无配置提交验证，未删安全断言或恢复旧入口。无真实数据操作，未部署；主库搜索/分类缺口、新经验巡检联动及最终集成迁移仍需收尾。回退点、允许列表、禁止部署的隔离代理配置与证据路径见 handoff。下条初轮失败为历史证据，已由本条定点修复覆盖。

- 三 Tab 初轮页面/权限核验（2026-09-19）：隔离后端库与 Owner 回归 67 passed、1 skipped（Windows 符号链接权限）；实际前端模拟 API，三角色 × 390/1440 入口、详情、分页及巡检入口隔离 6 passed，共享独立回归 4 passed。新增保留 library-journey.spec.ts，无产品源码或真实数据改动。**全旅程尚未通过**：窄屏标题说明被按钮挤成竖排（截图确认）；巡检错误被空 catch 隐藏（源码确认）；学习说明及均分指标仍偏旧链路；主库搜索/分类筛选缺失。旧文件上下文广组至少四项用例仍查找已隐藏的手动控件，已停止重复超时，不计通过，不恢复旧配置来迎合测试。证据及后续隔离修复边界见 handoff。

- 新本地模型失败教训学习隔离完成（2026-09-19）：复用新任务标记、冻结执行绑定、生成认领和两阶段用量记录，明确业务失败才生成本人草稿；历史、无结论和基础设施故障不生成。教训学习用量不抵消原执行未知调用。公开接口业务分支先红后绿，原外部连接及工作记录组合最终 32 passed；编译通过，定点差异仅行尾告警，Standards/Spec 无必修项。全为隔离数据库与模拟网络，未部署、无新增 DDL、未修改真实历史。下一步为三个 Tab 完整旅程、权限及巡检联动与最终集成部署门；下文各已完成子片保留当时证据，旧待办以本条及后续最新条目为准。

- 新本地模型方法学习隔离完成（2026-09-19）：用户确认历史不补学，新建任务不增加用户配置。新版创建标记是唯一历史边界，模型/端点来自确切执行快照，缺失或身份不符跳过，不切换当前默认模型；仅正式交付后生成私人草稿。调用前持久未知用量，完成后追加同调用结果，显示按 Revision/Run/call_id 合并；失败或结束落库中断保留未知且不重发，原执行未知调用不会被学习用量抵消。公开接口先复现本地无学习、审查发现崩溃用量缺口、补测发现执行用量被学习抵消，均修复。最终本地六场景、工作记录、原 Broker 生成组合 28 passed，编译通过、差异检查仅行尾提示；Standards/Spec 最终复核无必修项。全程模拟网络，新增测试入口默认阻断真实异步 HTTP；故障注入不等于真实杀进程验收。未部署、无新增 DDL、未迁移真实库；本地失败教训生成及三个 Tab 全旅程仍待，基线/允许列表见 handoff。

- 显式上下文与自动教训组合隔离修复：创建接口此前因已有 context_selection 跳过所有自动召回，现先沿用原确认哈希门，再只补充适用教训；已选模板、记忆及建议不被替换。目标或格式变化时不继承旧自动教训，相同目标重试保留快照且不回读删除原件。两处业务缺口先红后绿；Standards 发现网页附加约束与确认原话不同，导致仅刷新来源也丢教训，公开接口先红后修复：原话用于匹配/确认，完整目标用于冻结/继承。最终上下文、权限、四种来源、显式来源刷新及教训反馈组合 41 passed；编译通过，差异检查只有行尾告警。Standards/Spec 最终复核无剩余必修项。隔离依赖缺失的补齐范围与基线见 handoff，初期收集失败不计业务验证。未部署、无真实数据库迁移，不代表真实模型或用户验收；下一步是旧直连方法学习兼容与三个 Tab 完整旅程。

- 隔离补齐正式交付后的逐教训有效次数回写：最多三条冻结引用分别使用固定回执种类，必须满足确切交付、唯一通过的 lesson_effect 检查、已纳入编译上下文、原件内容与权限一致；重复恢复只计一次，原件删除/变化不复活不覆盖，沿用既有晋级/退役规则。仅修改未部署 webui_0022 的 CHECK 及对应清单摘要，没有运行库迁移。回执/反馈 26 passed（含隔离迁移恢复），另公开任务接口接线 1 passed；模拟验证器报告后正式交付与历史刷新统计一致，不能冒充真实模型验证。编译通过；定点差异检查仅行尾告警。Standards/Spec 无必修项，新增接口测试复核通过。源码仍在 mangrove-learning-stage-20260917-235613，基线见 handoff；显式上下文组合、旧直连生成、三 Tab 全旅程和部署仍待。下列证据门子片“未接线”描述是其完成时边界，本条更新当前接线状态。

- 隔离新增逐教训独立证据门：只读取确切 Owner/Task/Revision 的冻结编译段落，复用原候选验证调用，不额外调用模型。可选评估须同时认定实际应用及对应风险通过，并逐字引用共同可见的候选/来源正文；无证据、格式异常、重复、任务失败、引用未知条目均不产生有效性凭据。重验清除旧凭据；不新增 VerificationReport 字段，不改变历史报告结构或主验证输入预算。审查发现跨任务上下文及元数据/截断外引用缺陷后，均先红后绿修复。候选验证与 Provider 重验组合 58 passed；最后收紧异常边界并保留原输入预算后，全候选验证文件 40 passed，编译通过。模拟 Broker 覆盖附加载荷和单次用量，未调用真实模型；保留依赖告警。此片**仅完成证据核验，正式交付后的逐教训幂等计数仍未接线**，原库与运行源码未变。

- 用户确认恢复自动匹配、复用和学习的低配置体验，覆盖三个模块并保留角色边界。用户另行确认保留现有巡检自动删除策略，不改为归档；未知归属历史仍不认领、不召回、不清理。完整覆盖清单见 `docs/plans/2026-09-18-library-learning-coverage.md`。
- 已复现并修复：单条统计字段非数值、非有限值、负数、非对象元数据或非法关键词格式会拖垮库读取。模板和教训共用校验拒绝异常条目，保留原件、不伪造统计，正常条目仍可列出与召回；单条读取同样防御非对象元数据。未扩展共享、管理员正文权限或巡检策略。
- 本轮最终隔离组合 154 passed、1 skipped（Windows 无符号链接创建权限），覆盖三角色库读取、共享确认及撤回、普通用户拒绝巡检摘要、管理角色摘要不含正文、旧模板/教训学习及巡检、显式上下文冻结与工作台接口。模型和外部服务为模拟；依赖告警保留。Standards/Spec 以开工副本审查无必修项；补充直接召回断言已复核。
- 新工作台无显式选择时，已接入明确 workspace 类型的学习方法本地关键词召回与精确快照冻结。用户目标、格式及权限不由模板覆盖；不额外调用 embedding。原采集模板不以 generic 冒充工作台类型，历史条目不改写。同目标重试沿用冻结方法，目标或格式改变不继承旧自动建议。审查发现的网页执行约束导致来源刷新误判目标变化已修复，并通过删除原方法后来源修订仍保留快照的公开接口回归。
- 本轮定向验证：上下文服务/接口 15 项、四类自动召回 4 项、旧模板学习 21 项通过，网页来源修订补测 1 项通过；编译和差异检查通过。组合运行曾长时间未结束，已停止，不计通过。scripts 旧测试不继承 tests/conftest，发现潜在未隔离 embedding 路径后停止该进程并补齐默认隔离；此前是否发生外部请求未经确认，不能宣称本轮全程零网络。重跑 21 项使用模拟模型和禁用默认外部召回。
- **隔离实现已验证，未部署**：学习回执 webui_0022 和正式交付后方法使用统计。按 Owner/Task/Revision/Run 幂等，文件前后摘要处理写入与提交中断，内容漂移不覆盖；独立验证次数与旧质量评分分开，接受初稿不计 QA 成功。双轴发现后台恢复、显式发布中断窗口与队列饥饿后已修复：显式发布前入队，两个发布出口共用回写，维护循环核验持久授权并轮转补记，不扫描旧完成任务。最终分组共 76 项通过（回执/迁移/库权限 42，上下文与四类任务 32，显式重验正常并发及发布落库后中断 2）；编译检查和 Standards/Spec 最终复核通过。一次更大的混合组合长时间未结束已停止，不计通过。工程替身不代表真实模型或用户验收。
- 用户已确认新工作台方法草稿自动试用、达现有次数门槛且使用均独立验证后转正。隔离实现按当前正文摘要统计真实运行与正式交付，未验证使用阻止转正；合并改变正文后重新累计本版本资格，文件累计统计仍保留，不伪造旧质量评分、不扩大共享。跨正文计数缺陷已先红后修复；最终上下文与回执组合 25 passed，编译检查及双轴定点复核通过。保留两条依赖告警；仅模拟工程验证，尚未部署。
- 隔离补齐方法草稿生成：正式交付且未命中已有方法时，以冻结目标、输出格式及通过的核验项目生成本人待试用建议；不冒充实际执行步骤复原，生成任务不计入试用次数。绑定原模型连接、Owner/Task/Revision/Run，用量沿 Broker 账本；生成前持久认领，未知不重发；去重/准备/落盘同锁。延迟模型公开接口先红后绿，方法生成改为完成后的独立受管单并发学习循环，不阻塞任务完成或原维护恢复。完成门只限制新方法生成，不阻断正式交付统计的中断补记。修复子任务清理吞掉取消后工作器停机等待问题。最终沉淀、失败、延迟模型、用量、未验证不学习、回执、库权限及显式发布正常/落库后中断组合 51 passed（46 deselected）；上下文组合 35 passed；编译、差异检查及两路定点源码复核通过。保留依赖和行尾提示；不代表全范围或真实模型验收，仍未部署。
- 隔离补齐教训召回与冻结：无显式上下文的新任务可按确切 workspace 类型、本地关键词自动读取可见教训，最多三条；沿用本人草稿重复试用门及平台 active 门。冻结 slug、摘要及原建议，创建前再验可见性/状态/正文；历史重开或同目标重试不回读已删除原件，改变目标不沿用。编译为独立 lesson 建议，凭据与控制指令不注入，成功不增加 helped_avoid。专项 15 passed（文档/表格/网页/混合、三角色、漂移、共享及撤回），库权限与原自动方法组合 42 passed（15 deselected）；上下文与编译关联组合中其余 29 项通过。首次普通用户夹具因管理员灰度、随后因缺本人连接被正确拒绝，已补齐隔离已开放模式及模拟本人连接后专项全过；没有放宽真实受众或模型权限。编译、差异与双轴定点复核通过，保留依赖/行尾提示。新增 lessons 字段默认空值，旧快照兼容，无额外 DDL，未部署。
- **未完成**：旧直连模型任务的新方法沉淀（无冻结 Broker 连接时跳过，不默认换模型）、教训可核验有效性回写、显式上下文与自动教训组合、三 Tab 页面及完整旅程验收、原库备份与正式部署。失败教训草稿与恢复边界已在隔离实现验证，不代表整个学习闭环完成。当前自然产生的旧采集模板不具备 workspace 类型，不代表已自动积累出工作台经验。用户已批准在源码/原库备份与隔离迁移验证后执行学习回执表的显式迁移；仅隔离副本已演练，原库尚未迁移，运行源码未切换。本轮隔离路径见 handoff。
- 未修改真实历史数据、迁移数据库、触发真实业务任务/巡检/邮件、提交、推送或手动重启。源码回退点见本机 handoff，仅可按本轮允许列表恢复，不能整仓覆盖其他 WIP。

- 隔离失败教训子片进行中：公开任务接口 3 passed，明确业务核验失败可生成本人草稿并按不同执行累计重复；无结论和基础设施检查不沉淀，不增加 helped_avoid。首次组合因把连接探测算作生成调用而失败，修正夹具计数后通过。审查发现新文件写入后回执未提交、原件又被删除时会恢复重建；已在共享回执层加摘要写入标记，四个方法/教训恢复用例覆盖原件保留与删除，完整回执组合 10 passed。删除场景先红后绿，编译和两路定点源码复核通过。标记后首次落盘失败也保守转 conflict，不擅自恢复或重发；不保存原件归档、不改巡检删除策略。
- 上述两项恢复缺陷已修复：本次效果独立冻结 last_failure 身份，文件写入同一 SQL 写事务核对报告摘要，重验后拒绝旧累计；前台不写学习队列，现有失败事件记录学习意图，后台仅补入带新版标记的确切 Owner/Task/Revision/Run。审查发现终态先于意图的窗口后改为“意图事件→修订终态→任务终态”，仍保留执行权限与版本门。队列故障及终态保存故障先红后绿；公开重验用例最初因规则未变化被正确拒绝，模拟规则更新后通过，未放宽产品资格门。最终失败教训、回执/迁移、方法沉淀、库权限组合 **61 passed**，保留两条依赖告警；编译、差异检查及 Standards/Spec 定点复核无新增必修项。终态故障证明意图保留，不等于真实进程重启验收。无新增 DDL；原库与运行源码未切换，完整学习闭环仍未完成。

## 自动化执行与北京时间修复（已本机部署，待用户验收）

- 创建和编辑计划显式选择可用模型连接，冻结连接版本；立即执行先持久化运行编号再后台执行，页面轮询状态、结果与通知摘要。未知停止不自动重试，也不能从界面复制重建绕开保护。
- 新计划按 Asia/Shanghai 计算触发，新时间写入带偏移；显示统一北京时间。历史无时区值保留并标注，旧计划不自动补跑，需用户核对后重新创建。迁移 scheduler_0004 仅增列，不改写旧值。
- 工程验证：121 项后端组合、7 项模型绑定测试通过；最终风险修复后 8 项针对性复测通过。自动化与分页 2 项隔离浏览器检查通过，最终模型页面再次复测通过；TypeScript/Vite 构建通过。Standards/Spec 复核无新增必修问题。所有执行、模型与邮件使用模拟数据。
- 用户确认继续后，本机原库副本演练、幂等重放、正式 scheduler_0004 迁移及旧数据逐表核对通过，9 条旧计划和 110 条运行记录保留。源码按本次允许列表切换，前端构建完成，后端监督进程恢复，8088 引用新静态入口。真实模型、自动化执行与邮件未做验收；旧计划需用户明确核对后重建。
- 原库恢复点在系统临时目录 mangrove-automation-deploy-20260918，scheduler-migration-before.db 及 receipt 为正式迁移恢复证据，before 为本次源码备份，frontend-dist 为旧构建。回退前需保全新增数据并停止完整后端监督树，恢复匹配代码与数据库，禁止整仓覆盖其他 WIP。未提交或推送。
- 既有依赖告警和构建大包提示仍存在。单实例顺序调度未扩展，事件触发尚未实现；隔离验证不代表真实供应商验收。北京时间与旧计划兼容决策见 ADR-0046。

## 自动化任务统一分页（2026-09-17）

- 定时任务、运行记录、单任务执行历史复用现有分页组件，默认 10 条，可选 10/20/50/100，显示总数、范围和页码；不足一页也显示控件。筛选和条数切换复位，末页数据减少时回退有效页。
- 执行历史复用既有带 Owner 过滤的分页接口，替代固定最近 50 条读取；不改执行、编辑、报告和下载逻辑，无后端或数据库变更。
- 工程验证：新增隔离 Playwright 回归先红后绿，覆盖三处分页、20/50/100 切换、筛选复位、末页删除、历史任务参数、390px 控件边界和页面异常检查；构建、差异检查与 Standards/Spec 审查通过。8088 静态入口已引用新构建。未执行真实任务、提交、推送或重启；用户验收待确认。

## 功能使用热度矩阵（2026-09-17）

- 按用户确认方案替换访问与使用、用户洞察中共用的功能排行：桌面三列、窄屏两列等高色块，颜色以当前汇总最大 PV 为基准分四档，零值中性显示，同时显示功能名和准确访问次数。不是功能×日期热力图，不推断未采集维度。
- 复用已有 Radix Tooltip，悬停及键盘聚焦显示 PV/UV，Enter/点击沿用模块明细下钻。提供色阶图例、焦点轮廓、空数据提示；不只靠颜色表达数值。仅此图表移除分页，其他运营数据列表仍默认 10 条、支持 10/20/50/100。
- 工程验证：先复现缺少矩阵的失败用例；18 项隔离浏览器回归通过，等高约束补测通过。覆盖深浅主题、1440×1000 与 390×844、键盘/鼠标提示与下钻、零值/各色档、空状态和其他分页回归。新增矩阵各色档对比度检查通过，Standards/Spec 无必修项。无后端、依赖或真实数据变更，未提交/推送或手动重启，用户视觉验收待确认。
- 范围外遗留：左侧趋势蓝色小字在浅色主题对比度 4.06，低于 AA 小字 4.5，已记录但未扩改；本轮不宣称整个页面无可访问性问题。
- 最终构建通过，8088 健康 200 且静态入口引用新构建；既有大包提示未扩大处理。

## 运营审计命名与列表分页统一（2026-09-17）

- 用户确认范围仅本模块。导航及标题改为“运营审计”；最近关键操作、登录记录、访问明细、操作记录、用户明细、用户时间线、功能排行与常用视图均复用统一分页，默认 10 条，可选 10/20/50/100。总数和当前范围靠左，条数、页码、前后翻页及总页数靠右；窄屏允许换行。趋势图不是数据列表，保留原时间段浏览。
- 筛选及条数变更回第一页，越界页继续钳制；其他模块分页不变。旧“运营与审计”名称在筛选、常用视图和日志只读投影中兼容，统计合并为新名称，不改写不可变日志，不改变角色权限。
- 验证：默认条数变更的红灯复现后修复；15 项既有页面组合及 1 项新增全列表检查通过，包含 100 条选项、刷新、下钻、抽屉、窄屏和页面错误检查；12 项后端接口回归通过，覆盖旧新名称查询、历史视图、默认 10 条、100 条及统计合组。构建、双轴修复复核、8088 健康与新静态入口检查通过。仅隔离模拟数据，未读取真实日志、迁移、提交、推送或手动重启；用户验收待确认。

## 工作台时间戳与历史执行用量修复（2026-09-17）

- 用户输入、追问、回复与完成结果沿用已有时间样式，读取对应记录的真实时间；历史对话补取已保存元数据，缺失记录明确显示“时间未记录”，不使用当前时间补造。
- 接受初稿会发布新版本但不新增模型执行。此前历史详情只查新版本 Run，遗漏已保存的来源用量；现在按冻结 owner_acceptance.source_revision、Owner 和任务读取来源执行，保留其版本、状态与用量，界面注明来源 Vn。当前版本事件订阅、进度及运行身份不改为来源 Run。
- 结果时间缺失时只允许同版本执行结束时间回退，不能用来源版本时间冒充新结果时间。无记录的旧用量仍诚实提示未知，不估算或改写真实历史记录。
- 工程验证：41 项初稿/执行轨迹后端测试、8 项浏览器组合通过；时间归属复核修正后 2 项针对性浏览器复测通过。测试使用隔离数据库与模拟数据，不执行真实模型或任务。本轮无数据库迁移、提交、推送或手动服务重启；用户实测待确认。
- 收尾：Standards/Spec 发现的版本与时间归属问题已修复复核。仅接受初稿的紧邻版本可回看来源用量，后续新版本不能继承；新增 V3 隔离接口断言通过。最终两项浏览器复测包含窄屏，构建通过，8088 健康及新静态入口确认正常；既有依赖及大包告警未扩大处理。

## 工作台结果复制、反馈与 Token 展示修复（2026-09-17）

- 沿用现有操作栏，覆盖历史会话、新任务对话、工作台追问与完成结果。点赞/取消在服务端确认后回显；点踩恢复原因及补充说明，失败可重试，刷新保留状态。
- 工作台反馈按 Owner、任务、版本、回答绑定，进入已有反馈管理。正文仍需管理员说明原因并留下不可变审计；追问关联该轮问题，不借初始目标冒充。公开隐藏的内部内容在工作台反馈审计中继续隐藏，正文按 SQL 字节上限读取。
- 回复复制原正文，正式结果复制所选输出的完整可读表示：表格为 TSV，文档为文本；逐页校验身份，超过 1 万条或 2 MiB 明确引导下载，不静默复制半份。剪贴板权限拒绝时保留兼容复制与失败提示。
- Token 只展示已有真实统计；任务结果注明当前任务版本范围。旧回复未保存统计或模型未返回时明确提示，不伪造零消耗，不将整任务用量冒充单条追问用量。
- 显式迁移 webui_0021 扩展原反馈表及审计表目标关联，保留旧行、反馈自增高水位和不可变触发器。用户授权后先完成原库副本迁移（100 张业务表原字段摘要不变）与恢复演练，再备份迁移原库；5 条旧反馈、0 条原反馈正文访问证据、30 个任务和 32 个任务版本均核对不变，完整性及外键检查通过。
- 工程验证：最终后端/迁移/审计组合 71 项通过；浏览器组合 14 项通过（复制、点赞/取消、点踩原因、保存失败/读取重试、刷新回显、历史身份、追问绑定、多页复制、漂移/大小保护、剪贴板兜底、窄屏及旧反馈管理）。构建和 Standards/Spec 修复复核通过，8088 健康及新构建入口 200；既有依赖与大包提示未扩大处理。
- 备份与工程证据位置见本机 handoff。未执行真实模型、任务、邮件或真实反馈写入；未提交、推送或手动重启。隔离验证不是用户实测验收，待用户确认。

## 概览分页条数与统一控件（2026-09-17）

- 最近任务与定时计划复用已有分页样式，支持 10/20/50/100，默认 10 条；切换条数、分类或状态回第一页，保留首末页边界与越界修正。
- 最近任务接口最大 limit 从 50 调整为 100，默认参数保持兼容；Owner 范围不变。其他页面仍使用原分页选项。
- 翻页等待仅保留同账号、角色、筛选下的总数；不把等待误报为零条或空状态，不在占位数据阶段修正页码。
- 接口边界与缺少分页控件的回归先失败再修复。概览接口 8 项通过；浏览器覆盖四种条数、任务/计划末页、筛选复位、慢响应焦点、三种角色、深浅色及窄屏，使用隔离合成数据，不操作真实任务。
- 最终浏览器组合 9 项通过（包含运营分页兼容回归），构建及 Standards/Spec 复核通过；既有大分包警告未扩展处理。本机静态构建已更新，待用户视觉确认；未提交、推送或改库。

## 续期日志用户关联修复（2026-09-17）

- 已复现访问凭证缺失/过期时刷新成功、审计 actor_id 为空。刷新接口在轮换校验通过后把已验证用户交给审计，不信任客户端 Owner 声明。
- 精确将 `/api/auth/refresh` 操作命名为“登录状态续期”；保留操作事件类型，不增加密码登录次数。不改写历史未知身份日志。
- 2 个回归场景先失败再通过；认证会话与运营公开接口组合 51 项通过，Standards/Spec 复核通过。既有依赖警告保留。
- 本机热更新已检测到两个源码变更，健康入口返回 200；未登录真实账号或触发真实续期验收，未改库、提交或推送。

## 运营与审计原型还原与分页（2026-09-17，待用户视觉确认）

- 上轮实现未获用户视觉认可。本轮以已确认的五 Tab 原型还原紧凑指标带、趋势/关注双列、分类型表格、右侧详情与用户时间线；沿用生产导航和真实统计，不复制原型示意数值。
- 日志与时间线复用服务端分页，支持 10/20/50 条、总数与首末页；用户汇总保留既有前 100 位范围并分页，不宣称能翻查全部历史用户。排行、时间点与保存视图均限量分页。
- 修复筛选复位、越界页回写、翻页滚动复位和时间线键盘焦点；恢复来源统计下钻，修正总览无关键操作时错误禁止导出、超时残留旧筛选数据、浅色表头对比度。
- 用户补报整页滚到底空白：外层应用固定视口，运营页建立定位/滚动边界，长表限高内部滚动。旧构建的隔离探针未复现原空白，不能断言已证明旧根因；新版几何/滚轮回归证明桌面和窄屏无文档额外滚动，侧栏保持完整。
- 工程证据：运营 15 项与侧栏 5 项隔离浏览器检查全部通过；构建、strict 静态审计、Standards/Spec 修复复核通过。核看桌面、深浅主题、窄屏、抽屉与底部截图；本机 8088 静态入口匹配新构建。
- 本轮只改前端与对应文档，不改接口、数据库或真实配置，不读取真实运营日志，不调用模型、不发送邮件、不重启、不提交推送。既有大包提示仍在；不代表真实用户数据验收。

## 运营与审计（2026-09-17，工程验证完成）

- 正式入口 `/operations`，五个子页面共用时间、筛选、详情和导出；管理员仅普通用户与本人，超管全平台。
- 新增 webui_0020 运营事件、追加结果、前台会话、个人视图与保留策略；不读取业务正文，不回填旧行为。
- 低敏登录、访问、关键操作和下载审计接线；角色变动收紧历史可见范围，查看/导出拒绝也留痕。
- 工程证据：57 项接口/迁移组合、69 项工作台/计划/兼容业务回归、13 项最终接口/启动预检通过；各轮有重叠，不相加。
- 10 项最终浏览器回归及此前 5 项导航回归通过，包含三角色、键盘、筛选下钻、保存/删除视图、导出拒绝、窄屏深色和 Axe。
- 构建、strict 静态审计与 Standards/Spec 最终复核通过；发现的失败留痕、对象关联、时间线及下钻口径问题均修复。
- 原库副本前向/重放通过，98 张业务表摘要未变，恢复检查 ok、外键违规 0；原库已备份迁移至 webui_0020 并再次核对业务表未变。
- 后端完整监督树已恢复，8088 健康检查及 `/operations` 返回新版构建；统计从启用时开始，在线/时长为心跳估算，旧数据不回填。
- 构建仍有既有大包提示，测试仍有既有依赖警告；可选系统接口指标及留存分析未纳入本轮五页切片。详细记录见本轮实施计划。
- 本轮无真实模型、邮件、真实账号交互或 Git 发布；工程结果不代表用户视觉验收。

## 侧栏主题入口紧凑化（2026-09-17）

- 移除独立外观行，账号右侧放置月亮/太阳按钮；收起导航时仍保留，悬停和键盘聚焦提示目标主题。
- 复用现有主题状态和 Tooltip，不增加依赖，不改变账号权限或退出逻辑。
- 6 项隔离浏览器回归通过：三角色、深浅主题、同行对齐、键盘提示、窄屏抽屉、收起导航、草稿保留和模拟退出失败；构建通过。
- Standards 与 Spec 复核均无新问题；以上为工程证据，待用户视觉确认，未执行真实退出、任务或外部发布。

## 历史会话输入区遮挡修复（2026-09-17）

- CollectionHistory 移除与报告共用滚动容器的 sticky 输入区，改为正文独立滚动、底部输入区正常占位。
  输入框更贴近底部，不再覆盖正文或在底部露出后方文字；新消息仅滚动正文，不滚动整个页面。
- 保留输入自动增高及超限滚动、历史会话追问、反馈和附件接线。窄矮窗口或资料过多时整列可滚动，
  正文保留最小阅读高度，避免底部操作不可达，也不单独裁剪输入区弹出菜单。
- 先以页面几何断言复现遮挡，再修复；明暗主题、390/1440px 宽度、长输入、报告最后一行及480px矮屏
  与原历史追问回归共5项通过。桌面及窄屏截图已核对，构建通过（既有大包提示仍在）。
- Spec审查未发现问题；Standards指出矮屏溢出风险，已加入整体滚动兜底与回归。无真实任务、模型或邮件操作。

## 全局导航与账号区（2026-09-17）

- 按确认原型使用独立深青导航，工作空间/管理分组；管理顺序为设置、反馈管理、用户管理，角色权限不变。
- 当前页以背景、指示线、字重区分；深浅主题、桌面收起提示、窄屏抽屉和底部账号选项复用现有组件。
  退出失败保留身份与输入，可重试；同路径账号设置跳转也关闭抽屉，Escape 分层关闭并归还焦点。
- 深色主背景与卡片层级同步调整；输入及示例布局、模型和任务行为不变。未新增依赖。
- 工程证据：11 项隔离浏览器定向回归通过（角色、主题、导航、键盘、窄屏、模拟退出失败及成功）；
  最终截图专项重跑 5 项通过，包含 Axe 检查。构建及 strict 静态审计通过，两轴审查发现的同路径抽屉问题已修复复核。
- 旧导航测试的概览接口 mock 已补到现有契约。一次扩大匹配的回归遇到已记录的“其他资料”旧入口测试失效，
  未将该测试算作通过，也未扩大本轮功能范围。构建仍有既有大包体积提示。
- 本轮无真实账号退出、模型调用、任务、邮件、数据库、重启、Git 发布。等待用户视觉验收。

## 自然语言定时与邮件交付接线（2026-09-17）

- 四分类中的定时、邮件示例改为直接填入/追加用户需求，不再弹出配置引导。缺少邮箱的示例不填造收件地址。
- 工作台任务前对话复用既有 Conductor 采集类自动化：明确时间后直接创建计划；缺时间先追问，
  澄清链保留真实用户原话。创建回执和计划编号保存到会话；同一请求不会重复创建。
- 新计划冻结所选模型连接与版本；每次运行复用连接授权，连接删除/失效不回退平台默认模型。
  已绑定计划不能通过旧模型字段偷偷换连接；旧计划保留原有行为，不猜测补绑。
- 即时与定时采集结果复用同一邮件出口：仅用户明确要求才发送，只发送当前业务文件，
  明确通过质量检查才允许自动外发；缺检查/失败/发送未知保留结果并显示提示。
  正式工作台结果的手动“发送结果”入口不变。
- 用户批准备份、迁移与重启。显式 scheduler_0002 增加绑定信息后，隔离集成发现连接版本是摘要字符串，
  追加不可变 scheduler_0003 改为文本，未回写已应用迁移。两次均先副本演练再备份本机迁移；
  最终只读比较确认 9 条计划及全部运行记录与首份备份一致，历史绑定列均为空。
  备份及恢复回执位于本机忽略的 data/backups/，副本恢复校验通过；当前 Schema 检查为 current。
- 工程证据：后端组合回归 77 项、迁移框架 47 项、最终发送摘要专项 3 项通过；两轴审查均发现
  自动外发缺少明确 QA 门，修复并复核通过。新增字段对历史回填的兼容缺口也已修复复核，历史快照
  仍固定 scheduler_0001，只有新字段确实为空才允许旧记录回填，实际绑定变化仍拒绝。
  桌面/窄屏明暗主题、示例追加、键盘及手动发送失败提示共 5 项 Playwright 回归通过，构建通过；
  前端入口及资源、API 健康检查均为 200，热更新监督已完成服务重载，数据库启动预检通过。
  模型与邮件均使用模拟服务，没有新建真实计划或真实发送。测试及界面均保留，未提交或推送。
- 范围限制：这里只接通已有采集类调度，不代表上传文件任务或任意工作台任务已有周期执行能力；
  真实模型的语义识别和真实邮件投递尚未验收。时间按服务器本地时区执行，创建回执明确说明。
- 安全记录：一次测试导入错误导致配置对象出现在工具异常输出，包含敏感字段；已告知用户并修复，
  后续测试限制异常回显。建议维护者轮换相关凭据；本轮未自行更换密钥，也未把值写入文档或 Git。

## 工作台四分类示例与输入框自适应

- 新任务欢迎区采用已确认的四分类、八个示例；同一详情区按需展开。填入时保留已有文字、模型和
  输出选择，只追加需求，不启动任务。定时、邮件示例已按上节修正为自然语言需求。
  原帮助中的详细示例、任务执行、上传与预览入口保持不变。
- 复用 TaskComposer 的自动高度逻辑：最高为 320px 与视口高度 40% 中较小者，超限使用内部滚动；
  删除文字回缩，宽度变化重新换行测量。四分类区域与输入框间距 24px，窄屏单列，不增加依赖。
- 浏览器定向回归 7 项通过：四分类/追加/返回草稿、长文本增高回缩/滚轮/短视口、明暗主题与
  390px/1440px、Axe、弹窗键盘与焦点、既有上传不抢焦点及 IME 不误发。首轮测试确认旧 160px
  上限与缺少分类后失败，修复后通过；双轴审查发现的定时文案偏差已修正复核。
- TypeScript 与生产构建通过，严格前端静态审计零问题；保留既有大包警告。不代表真实模型执行
  或邮件投递验收。扩大检查的旧“输入网址传递要求”用例仍依赖已退役的“其他资料”入口，失败已知，
  未删除该测试、未声称全量回归通过。未改真实数据、调用模型、发邮件、重启或 Git 发布。

## 本地上传原件分类型预览

- 保留“添加文件 → 上传本地文件”和右侧预览外框。新上传 Excel 使用原工作簿窗口，支持工作表切换、
  行列标记、基本字体/底色/合并单元格、日期格式、每窗 100 行 × 30 列、滚轮及拖动；未缓存公式明确标注。
  DOC/DOCX、PPT/PPTX 在断网、只读、限资源容器中转为 PDF，支持翻页、缩放及拖动；不修改原件。
- TXT 支持保留空白、编码切换；HTML 经 DOMPurify 清理并用禁脚本 CSP/sandbox 静态呈现，禁止外部资源请求。
  Markdown 不执行原始 HTML；PDF/图片延续原预览，修复 PDF 缩放裁切并补拖动模式。失败保留原件下载和重试。
  历史任务冻结来源/证据视图未改为这条新上传链路，权限与任务执行接口不变。
- 已验证：后端 17 项全部通过，包括真实合成 DOC/DOCX/PPT/PPTX 隔离转换、中文/页数及原件字节一致，
  XLS 合并跨窗、XLSX 样式/公式/超限/Owner 拒绝；浏览器 12 项通过，覆盖桌面/390 窄屏、HTML 阻外发与拖动、
  Excel 切表/翻窗/展开状态保留/零自动下载、既有 Word/PDF/图片。双轴复核已关闭发现项，不代表用户原文件验收。
  TypeScript 与生产构建通过，8088 页面及新静态资源均为 200，新预览路由已热加载，转换容器无残留。
- 依赖：DOMPurify 3.4.15、xlrd 2.0.2；Office 需 Docker 和 `docker build -t mangrove/office-preview:local -f docker/preview/Dockerfile .`。
  本机镜像已构建并真实使用；本机 LibreOffice 仅用于生成旧格式测试文件，业务转换不直接使用宿主 Office。
  npm audit 仍有 4 项原有依赖告警，本轮未强制升级无关依赖。
- 边界：Office/Excel 原件 20 MB；XLSX 解压 32 MB、单元格与合并面积累计十万；文本 4 MB。
  超限明确提示下载，非无限大文件预览。Excel 非完整 Office 排版引擎，复杂样式/图表不保证；PPT 为静态页无动画。
- 修改前已建立逐文件 SHA256 校验的本机回退点，操作范围见 `.artifacts/preview-rollback/preview-rollback-notes.md`。
  未改真实数据库、调用模型、手动重启、提交推送或关机；下一步为用户视觉验收，不能重放旧发布/关机授权。

## 历史资料列表数据库锁冲突修复

- 本机复现历史列表在逐项核验正式输出时抛出 sqlite3.OperationalError: database is locked，
  原因是外层列表事务/未读完游标持有读锁，同时核验使用其他连接，阻塞并发提交。
- 列表去掉跨核验长事务，先取完游标再核验；不绕过 Owner、发布 QA 或选择时的字节核验。
  不删除或修改历史业务记录，分页仍用内容 token 检查变化。
- 新增公开列表 API 并发提交回归，先复现锁异常后修复；历史复用 API 16 项全部通过。
  本机账号原有 212 条资料成功读取，未输出正文；后端自动热加载后 8088 OpenAPI 返回 200。
  未真实采集、调用模型、迁移、手动重启或 Git 发布。

## 任务资料入口合并

- 移除输入区“其他资料”和新建手动网页配置入口。“添加文件”合并本地上传、选择历史任务文件，
  选择后关闭菜单并保留焦点；旧任务网页快照、在途网页草稿恢复和历史来源底层能力未删除。
- 自然语言指定网址原样进入既有草稿执行接口；未新增配置页或关键词强制路由。历史文件继续走
  原 Owner 核验、失效提示和引用冻结，未重传文件或重新采集历史快照。
- 合成 Playwright 17 项通过，最终补验 2 项通过；包含真实文件选择器触发、网址原话提交、历史三类
  资料、权限失效、取消/迟到、390/1440 深浅主题。TypeScript、生产构建通过；8088 新静态资源 200。
- 未调用真实模型或抓取网页，不代表真实采集质量验收；未运行整个工作台测试集。旧手动网页入口
  的存量浏览器用例仍需按退役后的历史恢复场景迁移，本轮未删除这些测试或冒称全量回归通过。

## 任务列表开关文案明确化

- 工作台顶部入口按状态显示“收起任务列表”或“展开任务列表”，配对应侧栏图标和键盘焦点样式。
  保留原开关逻辑与 aria-expanded；弹层、删除对话框的焦点恢复改用稳定元素标识，不依赖按钮文案。
- 既有 Playwright 合成页面验证 390/1440、深浅主题共 4 项通过，包含桌面键盘切换与窄屏 Escape
  返回按钮；截图目视核对无溢出，Axe 检查通过。首轮旧导航定位歧义已修正后复验。
  TypeScript 与生产构建通过，保留既有大包警告；8088 首页及工作台新资源 HTTP 200。
  未调用真实模型、修改任务数据或 Git 发布。

## 2026-09-16 错误本地型号退出当前目录

- 用户确认没有 Qwen3.8-27B-FP8，正确型号为 Qwen3.8-27B。公开目录排除平台旧导入版本中错误的
  FP8 条目，概览与模型选择器沿用 current_catalog 过滤；不写死数量，不删除数据库和历史绑定。
  此兼容规则不影响后来独立配置的同名模型或通过配置编辑产生的新版本。
- 三角色 API 回归先复现失败再通过；模型连接与配置编辑共 59 项、概览浏览器 7 项通过。
  本机原库只读投影核对当前型号为 Qwen3-30B-A3B、Qwen3.5-35B-A3B、Qwen3.6-35B-A3B、
  Qwen3.8-27B，共 4 个；未修改真实配置、调用真实模型、迁移、手动重启或 Git 发布。

## 2026-09-16 概览任务与平台状态重构

- 概览以当前 Owner 的任务元数据统计、筛选和分页，管理员不因此获得他人业务内容。近 7 天完成数
  使用任务完成事件或最后回复时间，重命名不改变完成日期；历史任务和会话直达既有工作台。
- 模型供应商使用对应标识，未配置凭据灰显；配置存在不冒充健康检查通过。平台服务三卡统一内容
  与底部操作基线，保留登录检查状态及角色对应管理入口，不返回 Cookie 原日志或密钥。
- 定时计划显示启用、暂停、计划时间与上次结果，直达既有管理页；计划时间投影带时区，页面按本机
  时区显示。新增三个概览只读接口，原任务和诊断接口契约保留。
- 完成窄屏导航焦点隔离、跨桌面宽度解锁、页面标题恢复、加载/失败/旧数据提示、15 秒读请求超时。
  合成浏览器验证涵盖三角色、390/768/1440 宽度、深浅主题、键盘、分页与定时任务跳转。
- 后端概览 8 项、模型连接 50 项通过；扩大浏览器回归 61 项通过，最后概览 7 项复验通过。
  TypeScript、生产构建、严格前端审计通过；保留既有大包警告。独立双轴发现的 4 项问题已修复并复核。
  实际截图与确认效果图同尺寸核对，服务卡内容和底部基线偏差测试不超过 1px，等待用户视觉验收。
- 8088 首页、index-DR3BO1Wj.js 与品牌资源 HTTP 200。未修改真实配置、真实连通性探测、模型调用、
  手动重启、迁移、提交或推送；合成测试不代表真实供应商连通性验收。

## 2026-09-16 语义召回配置直达

- 诊断入口与平台配置服务统一为“知识检索（语义召回）”；导航链接携带非敏感服务标识，进入
  “存储与知识”并高亮、聚焦对应配置。刷新保留定位；手动切换分类和后台刷新不反复抢焦点。
- 使用既有 Playwright（Browser 插件不可用）和合成接口验证 1440/390 桌面与窄屏跳转、刷新、编辑、
  分类切换，无真实保存或验证调用。截图目视无截断、乱码；目标页面无运行时错误或框架覆盖层。
- 平台配置 24 项回归首轮 23 通过；超管入口一项因 ERR_NETWORK_CHANGED 导致前端模块未加载，
  已查 trace，未改代码复验通过。另两项管理角色诊断/通知回归通过，共 26 个不同用例最终通过。
  TypeScript 与生产构建通过，保留既有大包警告。
- 8088 页面及 index-CT0g6Cd_.js 返回 200；未改真实配置、调用模型/向量服务、迁移、重启或 Git 发布。
  等待人工检查，不将合成接口验证等同于真实服务连通性验证。

## 2026-09-16 扩展工具管理展示简化

- 设置入口“能力治理”改为“扩展工具管理”，说明用于任务工具/MCP/技能包，不是报告质量校验。
  普通用户仍无入口且不加载治理接口，管理角色保留原验证、平台候选/发布及正文审计流程。
- 使用后端 audience 显示个人专用、管理员试用、普通用户开放；未知范围明确标注。隔离、撤销、弃用、
  待验证与已验证分别展示，不用单个 eligible 字段冒充无条件可用。安全检查失败与验证待办保持可见。
  完整摘要、三轴原始状态、扫描器信息与平台候选发布详情按需展开，原生详情支持键盘。
- 当前治理接口未返回可靠用途说明，页面标注“用途说明暂未提供”，不依据包名编造。未增加跨 Owner
  manifest 或业务内容读取，不修改后台治理、装载检查、权限和真实能力状态。
- 验证：53 项浏览器用例覆盖设置、三角色、验证/发布/审计和新增 1440/390 窄屏、键盘、Axe、错误重试。
  最终完整轮 52 通过，1 项因浏览器 ERR_NETWORK_CHANGED 未加载模块；该项不改代码单独复验通过。
  初轮发现新测试 overview mock 不完整及旧文案/容器定位失效，已修正；不将重跑次数重复计数。
  TypeScript、生产构建与严格静态审计通过；保留既有大包警告。双轴源码审查无剩余必修。
- 8088 首页与新资源 index-DmoIwIeJ.js 均返回 200；截图目视核对无截断或乱码。所有验证使用合成数据，
  未发起真实能力验证/发布、修改配置、重启、迁移或 Git 发布，等待用户人工检查。

## 2026-09-16 配置说明去重

- 服务说明复用已有分组指南，不再把同一套步骤按字段重复展开；SMTP、Slack 各展示一套配置步骤。
  两者的字段“如何填写”改为单项要点，管理员完整指南也同步去重。
- 新增桌面 1440 / 窄屏 390 回归：修复前 SMTP 显示 7 套步骤，修复后各服务仅 1 套；验证切换不串内容、
  字段帮助不丢输入、Escape 焦点返回、无横向溢出及弹窗 Axe 检查。平台配置浏览器回归 22 项通过，
  TypeScript 与 Vite 构建通过；构建仍提示已有大分块告警。截图人工检查无重复或乱码。
- 8088 首页与新静态资源 `index-BeuJ864-.js` 均返回 200；未修改真实配置、调用真实模型或发送通知，
  未重启、迁移或 Git 发布。等待用户检查。

## 2026-09-16 用户授权的报告邮件与 Slack 通知

- SMTP 与 Slack 从历史只读改为“平台配置 → 通知”，管理角色可编辑、启停和检查。旧采集默认模式、
  未开放保留参数仅隐藏入口，后台值不删除。SMTP 检查只登录；Slack Bot 检查只验证身份，不发测试消息。
- 正式结果新增“发送结果”：选择收件邮箱/已配置 Slack 频道、正文及真实文件附件，发送失败保留报告。
  Webhook 仅支持正文；附件需 Bot Token、频道 ID 和相应权限。显式指定频道不得回退到未知频道的 Webhook。
- 首轮自然语言发送需求、已完成任务的纯发送追问和收件人补充均已接线；混合修改要求仍走原修订流程。
  授权只取冻结的用户原话，不取模型合成目标或资料内容；新修订不继承旧发送授权，接受初稿例外沿用
  同一次任务授权。旧 chat 仅延续服务器保存的未完成澄清链，不扫描已完成历史补发。
- 复用 Owner/任务版本/正式输出校验与持久执行领取；同一目标和文件重复提交不重发，未知结果不自动重试。
  SMTP 线程取消等待收口；网络写入前复查账号、来源和通知配置。发送回执不改变报告的正式发布结果。
- 附件总大小上限 20 MB。正文取已发布文本报告，无文本时提示选附件，不用质量摘要冒充正文。
  SMTP/Slack 接受请求不代表收件人已阅读；部分投递或中断只显示未知，需核对接收方。
- 依据用户最新授权新增 ADR-0044，部分替代 ADR-0039 的邮件/Slack 禁止项；外部数据库和社交写入仍禁止。
  新增 Slack Bot Token 仍用 SecretRef，当前秘密配置键 26 个；不改历史迁移，不需要新增 DDL。
- 验证：隔离后端扩大回归 123 passed；补齐通知澄清后专项 11 passed。TypeScript、生产构建通过，保留既有
  大包警告。设置/平台配置浏览器 51 项通过；最终工作台完整 25 项通过（含桌面/窄屏发送、Axe、结果保留）。
  双轴源码复核无剩余必修项。开发热更新曾干扰浏览器加载；生产预览中 24 项通过、1 项需导入开发模块的
  测试不适用，最终固定 5173 开发页面后工作台整轮 25 项通过。未把重复复验相加为独立覆盖数。
- 8088 页面与健康检查 200，返回 index-DYeoYj1h.js，OpenAPI 已包含任务 notify 路由。现有重载器加载后端，
  未手动重启。所有外发和模型测试均为模拟；未修改真实配置/密钥、真实投递、迁移、提交、推送或关机。
  下一步：用户刷新 8088 设置页进行人工验收；真实 SMTP、Slack 权限及实际模型识别仍需明确授权后验证。

## 2026-09-15 检查管理分区与列表对齐

- Cookie 检查管理使用独立浅色背景与强调边框；定时巡检移入管理区，保留编辑、说明与搜索入口，不再混排在账号列表。
- 检查结果统一为状态/时间与日志两行；长日志单行省略，点击打开完整日志弹窗。历史检查事实与本次检查结果分别保留，不改变后端检查语义。
- 网络与浏览器、存储与知识等共用固定操作列和三槽位，避免按钮数量不同导致摘要起点漂移。
- 复核修复旧布尔字符串 true/1/yes/on 等显示与表单回填，避免已启用巡检误显关闭；不改写真实配置。
- 隔离浏览器回归 50 项通过；布尔修复后受影响 19 项再次通过。TypeScript、生产构建和严格静态设计检查通过，保留既有大包警告。
- 后续按用户反馈仅缩窄 Cookie 名称列，让状态和日志左移，右侧操作列不变；新增横向位置断言，19 项相关浏览器回归与生产构建通过。
- 8088 返回 index-DClnSWVW.js，待人工检查。未修改真实配置、真实执行采集检查或进行 Git 发布。

## 2026-09-15 紧凑服务列表与配置指南恢复

- 平台配置从大卡片改为紧凑服务行：桌面按名称用途、摘要状态、操作排列，窄屏自然堆叠；保留分类、搜索、未知配置兜底及 Cookie 批量检查。
- 每项“配置说明”复用原有逐字段指南和获取链接；编辑字段旁的“如何填写”按需展开且保留输入。只读历史参数继续只读，不恢复退役模型入口。
- 修正本次接回指南中与当前界面冲突的空值清除、服务器地址、随意填写密钥、旧检查点入口等说明；未开放的制品清理补充明确说明。
- 隔离浏览器回归 47 项通过；追加桌面/窄屏指南交互与 Axe 检查 2 项通过。TypeScript、生产构建、严格静态设计检查通过，双轴源码复核无新增必修。保留既有大包警告。
- 8088 已返回 index-C_-iSULw.js。未修改真实配置、调用真实服务或执行 Git 发布；第三方获取入口未进行真实登录验证。

## 2026-09-15 卡片操作与共享 Cookie 批量检查

- 卡片展示服务用途、配置摘要和状态，移除重复说明与查看折叠；底部统一编辑/检查，历史只读项用详情窗口查看。
- 共享账号新增“一键检查 Cookie 状态”：一次确认后顺序检查全部已配置共享 Cookie，跳过空配置，逐项显示结果与进度。停止按钮只停止后续请求，不承诺中断当前服务器请求；离页、网络结果未知时不发下一项，不自动重试。
- 隔离浏览器回归 46 项通过；最终精简 Cookie 结果区域后受影响 15 项再次通过。TypeScript、生产构建、严格静态设计检查通过，双轴源码复核无新增必修。已有大包警告保留。
- 8088 返回 index-C8-9Z2W8.js。未真实访问采集平台、修改真实配置、提交或推送；实际 Cookie 有效性仍须管理员在页面主动检查。

## 2026-09-15 平台配置分类卡片布局

- 改为搜索采集、共享账号、网络与浏览器、存储与知识、历史兼容五类；桌面卡片网格、窄屏单列，搜索跨分类。
- 未识别的服务分组和搜索字段保留兜底入口。模型五组继续由模型页面统一维护；历史通知只读，制品保留参数只读标注未开放，不启用自动清理。
- 浏览器隔离回归 42 项通过，包含分类切换、卡片并排、跨类搜索、未知配置、权限、键盘和异常处理；TypeScript 与生产构建通过。构建仍有既有大包警告。
- 8088 已返回新资源 index-Bq1hgMlr.js。未修改真实配置、调用真实模型或执行 Git 发布。

## 2026-09-15 平台配置服务级维护与文案精简

- 平台配置按服务编辑：同一服务一次填写、一次保存；地址回填、密钥默认隐藏且可切换显示，
  留空保留原密钥，秘密的首尾空格不改写。模型维护仍只跳转“模型与连接”；诊断不再重复提供配置开关。
- 新增成组保存 API，先整体校验再单事务写入；SecretRef 仍走原加密存储。同进程保存与重置串行覆盖
  落库和 settings 更新，避免运行态与数据库逆序。无需迁移；不代表多进程配置广播或编辑版本冲突检测。
- 恢复默认须确认具体字段及回落值（含合法 0，秘密仍掩码）；空值、重复提交、错误与未知结果有提示。
  地址检查不冒充 Key 验证；外部检查先展示范围并确认，结果留在服务内。已配置重排失败时组合检查不再报通过。
- 代理按供应商显示字段，SQLite 不显示 MySQL 编辑字段或检查。巡检只配置计划、不提供假验证；
  知识维护保留删除草稿风险提示。历史通知只读、旧采集模式准确标注，未接线保留天数不提供编辑。
- 按用户最新要求压缩页面说明：分区说明只出现一次，详细检查影响放在确认框；复用既有主题、Button/Input/Modal，
  未另建视觉系统。补齐加载/重试、搜索清除、键盘焦点、窄屏当前导航可见性与字段错误关联。
- 验证：`test_platform_config_services.py` 与 `test_runtime_config_secret_refs.py` 共 33 passed；
  旧 SecretRef 测试夹具更新至当前 webui_0019，不修改真实库。新增服务页面与既有设置角色浏览器回归
  最终整轮 41 passed，包含三角色、窄屏、键盘、Axe 与失败恢复。前期模型页两项失败单独及最终整轮复验通过。
  TypeScript、生产构建通过（既有大包警告）；设计严格静态审计 0 findings；Standards/Spec 复核无剩余必修项。
- 浏览器/API 验证均用合成数据与隔离数据库，未调用真实模型或采集服务、未更换真实配置或密钥。
  现有重载器已加载 batch 路由；8088 健康与页面 200，提供 `index-BWQl7ZAr.js`。未手动重启、迁移、
  提交、推送或关机；用户可刷新进行真实账号人工检查，工程验证不等于真实供应商验收。

## 2026-09-15 模型配置统一编辑与版本安全

- 模型维护仅在“模型与连接”，平台配置保留跳转并隐藏旧模型参数编辑；后台兼容参数和旧任务读取不删除。
  云端支持地址、API Key、模型与默认型号，本地 Qwen Chat 增加默认/开启/关闭思考；非支持协议不展示假开关。
- 编辑先验证精确草稿，再追加连接版本。Key 留空保留，旧秘密和冻结任务不覆盖；替代版本退出新目录，
  旧默认保留身份并要求重选。地址变更须确认，权限、版本冲突和版本链删除失败关闭。
- 验证操作按用户和操作 ID 幂等；未知结果只读核对，不重复请求供应商。刷新仅保存操作 ID，不保存 Key；
  输入拒绝可修改，未知操作可经明确提示重新编辑。保存与重复提交、并发编辑均有回归。
- 隔离后端模型/API 回归 57 项通过；新增配置用例最终 6 项通过；迁移、组件、模型配置及 Provider 回归
  59 项通过。初稿、聊天、采集质量与进度等相关回归 86 项通过；扩大后端回归先通过 183 项、跳过 2 项，
  因测试中更新迁移清单产生的进程缓存差异，重跑剩余三文件 94 项通过。两项跳过为真实 Docker opt-in 与 Windows 符号链接权限。
- webui_0019 最终脚本在数据库副本迁移、恢复副本 integrity/FK 检查通过，五张既有业务表双向差集为空；
  获授权后本机备份并迁移成功，状态 current，无 schema 缺口。备份与回执保留在忽略目录，不进入 Git。
  后端隐藏启动，8088 页面/健康 200；生产构建 index-DhqfB7F7.js，TypeScript 与构建通过（既有大包警告）。
- 三角色、窄屏、键盘、Axe、未知恢复及工作台浏览器回归共 223 项闭环：完整运行 222 passed，
  最后 1 项补齐刷新读取模拟接口后复验 1 passed。旧用例入口已适配折叠菜单、单选模型与自动预览，
  合成页面增加未声明 API 的统一 404 拦截，避免请求落入真实服务；既有权限、隔离、取消断言保留。
  minimum-ci 快速契约门本机 66 passed、1 deselected、17 subtests passed；UTF-8 检查 1428 文件通过，
  暂存区 Gitleaks 扫描未发现泄漏。Standards、Spec 双轴已报告问题均闭环，无剩余必修项；
  复核为代码审查，测试由主任务执行，工程验证不等同于真人验收或真实供应商认证。
  本轮未调用真实模型或更换现有密钥。决策见 ADR-0043；此前初稿和工作台已完成改动纳入同次授权推送。

## 2026-09-15 平台模型维护统一入口

- 取消独立“连接维护（高级）”列表，管理角色从统一型号行点击“管理”；同型号只显示一次，
  确有多套配置时在管理弹窗选择真实连接记录，停用、重试、删除仍绑定具体连接。
  平台配置不再重复列模型，改为跳转该唯一管理入口；旧流程运行参数仍保留兼容，不迁移后端。
- 本地旧导入记录按当前本地配置目录过滤（成功空目录与读取失败区分）；新建自定义连接不受该
  过滤影响，异常型号仍可恢复。当前目录之外的旧云端型号不进入管理卡片。历史记录未物理删除。
- 只读公开模型元数据确认仍有旧 FP8 失败导入与重复 Qwen3 记录；未改真实配置、密钥或数据库。
  去重和管理关联共用来源分组，删除成功关闭管理弹窗；单型号不再显示连接首选设置。
- 隔离 Playwright 8 项通过，窄屏调整后追加 3 项通过；覆盖三角色、重复/退役/空目录、异常自建、
  不同本地接入记录、默认不串改、停用确认、删除防重与统一跳转。管理弹窗 Axe 与截图已检查。
  最终窄屏宽度修正再验 1 项通过；TypeScript、生产构建通过（既有大包警告）；8088 页面/健康 200，引用 index-CRV0VsUk.js。
  双轴复核发现的分组差异、删除悬空和空目录过滤均已修复并复核通过。
- 未调用真实模型、重启、提交或推送；下一步刷新 8088 进行真实账号人工检查。

## 2026-09-15 平台模型清单归一

- 平台页复用任务选型去重/当前目录过滤，仅显示本地、云端两类，云端按供应商分组；
  不展示“导入的”内部名称。只读目录核对当前为四个可用本地型号、阿里百炼及 DeepSeek 两家云端。
  旧云端目录、重复选项、未通过验证型号移出日常清单；不物理删除连接或历史任务绑定。
- 管理员连接维护折叠保留，展示规范名称和短连接编号；不能用可用清单过滤误伤失败型号重试、
  停用型号启用或重复真实连接维护。自定义供应商以原始身份去重后再简化名称，避免跨供应商误合并。
- 定向隔离 Playwright 8+1 passed，覆盖三角色四本地/两云分组、去重退役、自定义同名型号隔离、
  管理维护、默认选择与异常恢复；平台清单 Axe 抽查无违规，已检查截图。
  TypeScript、生产构建通过（既有大包警告），8088 引用 index-BG2D7Frv.js，页面及健康检查 200。
  双轴审查发现的维护过滤及自定义供应商误合并均已修复并复核。
- 未修改数据库、模型配置或密钥，未调用真实模型、重启或执行 Git 发布；下一步人工刷新检查。

## 2026-09-15 模型与连接体验优化

- 默认先展示平台连接；个人模型为可选添加。复用任务默认选择器及既有身份去重/权限过滤，
  隐藏重复引导和高级入口，添加表单折叠自动名称、收起默认区域，把测试计费提示放在提交前。
  保存后区分“去工作台”和“设为我的默认模型”，不会自动修改个人偏好。
- 管理员云端/本地入口使用明确名称、自动填连接名称并说明共享范围。本地流程先地址/鉴权、
  再读取/选模型；切换端点只清除旧发现结果，手填保留并重新验证。普通服务隐藏协议字段。
- 停用平台连接先说明对全部授权及运行任务的影响；删除、停用增加提交锁。父连接停用时
  子模型不再显示可用或提供任务默认选择。连接内部首选放入高级设置，个人默认保持独立。
- 保存结果未知时可只读核对列表，用户明确确认后才允许再次测试；收费探测同样受锁约束。
  所有范围保留加载失败与重试入口，不把接口失败误报成空列表。补 Tab 方向键/焦点、
  中文状态、明暗状态徽标对比度，移除无法操作的密码眼睛图标。
- 隔离 Playwright 最终 15 passed：三种角色、个人多账号、平台添加、逐模型重试、未知结果、
  停用确认、删除防重、端点失效与手填保留、高级首选实际切换且不改个人默认、引导与加载异常。
  桌面 1440×1000、窄屏 390×844 截图及键盘检查通过；抽查明暗主题和列表/表单 Axe 无违规，
  三角色截图用例无页面异常。测试 API 均为合成拦截，不能替代真实服务商验证或人工验收。
- TypeScript 与 Vite 生产构建通过（既有大包警告仍在）；8088 页面、健康检查 HTTP 200，
  页面引用本次 index-BNHGjhib.js。Standards 原 1 项、Spec 原 3 项复核问题均修复；
  补充了高级首选实际提交与个人偏好零写入回归。本轮未修改后端、真实配置或运行服务。
- 未完成能力：现有连接原地更名/换 Key 的版本化接口、服务商结果自动对账、跨刷新未知操作恢复；
  不把“已刷新列表”当作费用或提交结果已确认。不新增依赖，不执行 Git 发布或关机。

## 2026-09-15 退出所有设备按钮可发现性

- 仅将设置页退出所有设备从弱化文字改为警示描边、浅红底、退出图标与至少 44px 高按钮，
  保留确认、忙碌禁用和异常反馈，增加明暗主题及键盘聚焦样式。
- 定向 Playwright 1 passed：按钮可见、键盘操作、取消确认后仍在设置且按钮可用；
  合成登录环境未调用真实退出接口。TypeScript 检查通过。

## 2026-09-15 旧对话入口收敛

- 用户同意隐藏旧页面入口：移除主导航旧版对话，概览会话卡进入工作台，移除重复采集按钮。
  保留 /chat 兼容路由、历史数据和全部后端执行接口；不代表执行链已经统一。
- 代码核对：工作台 collection 意图转入 chat_stream / Conductor（LangGraph）；
  语义任务经 AgentKernel 选择冻结 Adapter。本机配置 CoreMind 关闭，默认 pi-runtime。
  本轮未修改运行路由或启用 CoreMind。
- 定向 Playwright 1 passed：导航隐藏、概览两个入口进入工作台、旧地址可显示输入框，
  无页面运行异常。首次测试因概览合成数据缺少 templates/providers 失败，补全后通过。
  TypeScript 与生产构建通过（既有大包警告）；未执行真实采集或模型任务。

## 最新现场核验：2026-09-15 管理员直连名称同步

- 用户允许同步旧直连名称并重载。本机环境配置只将 Qwen3.8-27B-FP8 改为
  Qwen3.8-27B，地址、其他模型、默认项与 Key 未变；旧连接记录不删除。
- 现有重载器记录环境文件变更并重启后端，随后 8088 健康检查 HTTP 200。
  独立进程读取模型目录断言正确 ID 存在、旧 ID 不存在通过；未登录浏览器验收。
- 未新增模型生成请求、提交、推送或关机。

## 最新现场核验：2026-09-15 Qwen3.8 正确调用 ID 已开放共享

- 用户进一步允许修正调用 ID 并额外验证一次。现有接口不提供已登记连接原地改 ID，
  因此先做本机一致性备份，经既有 Broker 用 Qwen3.8-27B 向同一已确认 LAN 端点
  发送一次极短验证；成功后登记并启用正确连接。旧 FP8 名称失败记录保留，不改写历史。
- 真实验证返回 available / enabled，Provider 用量已记录。以 can_manage=False 查询共享
  清单，四个本地模型均可用：Qwen3-30B-A3B、Qwen3.5-35B-A3B、Qwen3.6-35B-A3B、
  Qwen3.8-27B。未发送业务资料；管理员原默认偏好前后未变。
- 尚未同步管理员服务器直连配置中的旧 Qwen3.8-27B-FP8 名称：本机 .env 被重载器监听，
  修改会重启后端，不能为名称清理擅自中断运行任务。后续须在获准重载时同步；管理员目前
  仍可能看见旧直连选项和正确共享连接。普通用户只使用共享清单，不受旧直连项影响。
- 本轮未修改产品代码、Key 或其他模型；未删除连接、重启、提交、推送或关机。

## 最新现场核验：2026-09-15 补验证普通用户本地共享模型

- 用户允许对缺少的 Qwen3.5 / Qwen3.8 各做一次极短请求，通过后启用共享。
  先做本机 SQLite 一致性备份，再通过既有 Broker 重验原有待验证连接，没有重复登记。
- Qwen3.5-35B-A3B 真实验证通过且启用；Qwen3.8-27B-FP8 返回 model_access_denied，
  仍未启用。后续仅只读获取服务端模型列表：该服务实际公布的调用 ID 为 Qwen3.8-27B，
  与登记 ID 不一致；不能据此证明其量化格式。没有第二次生成请求或强行标记成功。
- 非管理权限的共享连接查询现含三个本地型号：Qwen3-30B-A3B、Qwen3.5-35B-A3B、
  Qwen3.6-35B-A3B。管理员原默认偏好前后相同；未发业务资料、改 Key、重启或提交推送。
- 待用户确认修正 Qwen3.8 调用 ID 并额外验证一次；此前“管理员四个可见”不代表四个
  均已向普通用户开放，也不能将短连接验证写成完整业务任务验收。

## 最新修复核验：2026-09-15 平台型号去重与归属区分

- 用户进一步要求同一平台型号只显示一次且去掉连接括号。共用选择器按平台型号去重，
  优先保留当前/冻结连接；底层连接不删除、不改写。个人连接不与平台连接合并；
  存在个人模型时以“· 我的 / · 平台”区分，只有平台模型时仅显示简洁名称。
- 设置页从完整目录随当前选择推导选项，刷新不丢未保存连接，取消可恢复原默认身份。
  平台配置新增本地/云端模型清单与管理平台连接入口，排除个人 Key；旧流程参数折叠保留。
  模型列表读取失败明确提示，不阻断其他平台配置；管理入口直接打开平台连接分页。
- 本轮只改前端与回归测试；未调用真实模型、改 Key、删除连接、调整数据库或手动重启。
  最终浏览器 **16 passed**：去重、归属、刷新/取消、历史冻结、角色入口、平台模型
  失败与跳转、桌面/窄屏以及键盘和三角色明暗主题可访问性；TypeScript 与生产构建通过。
  较早的三项旧用例失败源于漏拦截接口触发登录失效及过期导航定位，已修正隔离与定位后
  最终整组通过。复核发现的去重后刷新丢编辑身份问题亦已修正，有红绿证据。
- 8088 更新为最终构建供人工验收。截图为合成数据，不代表真实业务运行；既有 chunk
  大小提示保留。未提交、推送或关机。下节同名连接长后缀展示方案已被本节替代。

## 最新修复核验：2026-09-15 模型两级分类、云型号更新与紧凑密码区

- 设置与任务输入共用模型选项：仅“本地模型 / 云端模型”两组，云端按供应商组织，
  本地按自然版本降序；同名连接标明“我的 / 平台”和连接名，仍重名时加公开连接标识。
  不合并不同连接的身份或端点。密码区默认收起，按需展开；取消清空密码与可见状态。
- 当日核对供应商官方目录：阿里采用 qwen3.8-max、qwen3.8-flash；DeepSeek 采用
  deepseek-flash（V4.1 Flash）和 deepseek-v4-pro。官方当前计价页注明 V4 Pro 继续服务，
  不能按较早公告将其误标为 V4.1。目录来源为阿里模型列表和 DeepSeek 当前计价文档。
- 用户明确允许使用已保存平台 Key 做极短请求后，先做本机一致性数据库备份，再通过既有
  Broker 分别新建阿里与 DeepSeek 平台连接；四个型号均真实验证通过并启用。
  未发送业务资料，未回显 Key，未修改/删除旧连接或个人默认偏好。备份只留本机临时目录。
- 旧型号/已被新平台目录覆盖的旧连接保留历史状态与冻结绑定，不进入新任务选型；
  历史任务编辑资料仅豁免精确冻结型号的目录筛选，仍检查连接验证、型号启用及可用性。
  若个人默认值指向退出新目录的旧项，界面提示人工重新选择，不静默替换。
- 最终回归：模型连接 API **50 passed**；浏览器 **11 passed**（桌面 1280×720、
  窄屏 390×844，合成接口）。覆盖两组、供应商、同名归属、保存/刷新/失败、密码取消清空、
  退出取消、角色边界、旧任务资料修订及新任务模型身份。历史目录过滤与归属标签均有先红后绿证据。
- Standards / Spec 复核发现的历史模型过滤和个人归属枚举问题均修正并复核。
  TypeScript、生产构建通过；仍有既有 chunk 大小提示。8088 设置页/健康 HTTP 200，
  页面引用最终构建资源。截图已查看；无相关页面运行异常。未登录执行真实业务任务，
  合成浏览器回归与极短模型连通性验证不等同于完整业务验收。
- 本轮没有手动重启、提交、推送、生产迁移或关机。下节“云端尚未验证”是本次授权之前的记录。

## 最新修复核验：2026-09-15 设置页与新任务默认模型

- 后续用户授权重启：仅重启已核对归属的 8088 后端，监督器恢复为新进程；数据库预检、
  8088 健康/同源页面/局域网监听通过，真实浏览器登录页正常且无页面异常。
  新进程启动晚于环境文件更新；配置读取包含四个本地模型。未登录检查实际下拉框，
  未调用真实模型、验证云端新型号或执行任务；前端开发服务与采集服务保持运行。

- 我的设置按任务默认模型、外观、登录与密码排列，移除旧对话流程默认模型展示。
  设置分区改为紧凑可横向滚动导航；密码支持显示与规则提示，退出所有设备先确认。
- 新任务与默认设置共用配置选项过滤；仅已验证连接的启用可用模型进入 Pi 下拉框。
  本地兼容选项仍只对管理员开放。旧供应商接口不再把未配置预设或历史基线冒充可用模型。
- 默认偏好可保存/清除，刷新保留未保存编辑，失败明确提示；无修改不能重复保存。
  本地偏好复核角色及当前配置；原偏好失效不替换；显式任务选型不被后端默认偏好覆盖。
  Legacy 有附件路径保留 Provider 与草稿运行模式，无附件不支持的 Legacy 云通道阻止误发。
- 本机忽略环境文件按用户提供的四个本地模型与地址更新，自然版本降序展示。
  未调用真实模型、未改写云连接冻结版本或自动启用新型号。云端最新型号的重新验证/启用
  尚未执行，不宣称已完成用户希望的最新云型号整理；本轮完成的是实际可用配置的筛选机制。
- 回归：模型连接/API 较宽组 **50 passed**；最终新增及受影响模型目录/偏好 **3 passed**，
  本地默认创建/显式覆盖隔离检查 **2 passed**；浏览器最终整组 **8 passed**。
  新增创建测试曾卡在模拟后台关闭，诊断为 TestClient 等待 shutdown；将本例限定为
  创建/冻结并隔离 enqueue 后通过，未据此改动生产停止逻辑，也不宣称真实运行速度已验证。
- TypeScript 与生产构建通过，保留既有大 chunk 提示；8088 健康 HTTP 200，5173/8088 监听。
  未主动重启，未验证运行进程已加载新增本地地址；前端构建已更新，真实模型与用户验收仍待完成。
  本轮无提交、推送、生产迁移或关机。

## 最新修复核验：2026-09-14 初稿 UX 审计后优化

- 用户接受初稿不再把未完成检查显示为通过；正式结果明确标注用户接受及未验证部分。
- 初稿首次在桌面非输入时打开；窄屏由用户打开。新版只提醒，不替换正在阅读的文件，
  不抢输入焦点；同稿保留文件、滚动与 PDF 页码，新稿不继承旧页码。支持扩大预览。
- 预览底部可采用当前稿或返回对话提出修改；确认展示冻结稿身份与文件，提交后不遮挡阅读。
  接受等待 30 秒无响应则显示结果未知，先只读检查状态；不会自动重发或重跑模型。
  发布成功回执不再等待后台查询刷新；切稿取消旧下载后恢复按钮。
- 新增冻结 PDF 按页图片预览；DOCX 为提取文字/表格，XLSX 为首表最多 50 数据行、
  50 列且合计 16000 字。明确部分预览边界；保留 Owner、来源保护与哈希检查。
- 后端合成隔离回归 **47 passed**：初稿接受/预览、快照及 Publisher；1 个既有弃用警告。
  真实模型、真实任务接受和运行中后端重载不在本轮验证内。浏览器使用既有 5173 与合成 API。
  此轮保留曾出现的页面加载超时证据，不能据单次复跑推断实际长任务性能。
- 静态 strict 审计 0 项；Standards/Spec 复核必修项已修正。正式环境人工验收仍待用户完成。
- 2026-09-15 收尾：最终整组浏览器 8 passed / 1 页面启动超时；将超时用例的虚拟时钟
  缩到接受请求范围后，冲突/超时两项复跑 **2 passed**。此前整组有 9 passed 记录，
  不将这些分批结果冒充最终整组全绿。桌面/窄屏、浅深主题预览 axe 检查通过，截图已查看。
  TypeScript、生产构建通过（既有 chunk 大小提示）；8088 页面/健康 HTTP 200。
  运行中后端是否已加载新接口未验证，未擅自重启。验收仍需覆盖真实初稿与完整会话。

## 最新修复核验：2026-09-14 初稿右侧预览与接受冲突

- 初稿生成后自动进入既有右侧画布，不再依赖对话区折叠详情；CSV 以有界表格展示，
  JSON 格式化显示，Markdown 保留文本预览。切换文件不调用下载接口，下载需独立点击。
  窄屏复用全宽画布，可关闭返回对话；其他未支持内联预览的格式明确提示独立下载。
  表格上限 50 数据行、文本上限 16000 字；保留 Owner、来源保护与冻结文件哈希校验。
- 复现后台维护对 `cancelling` 重复登记取消代数并因缺少冻结执行授权失败，导致接受流程
  被新取消代数阻断。维护改为沿用任务执行绑定，仅恢复已有停止意图。
  已停止任务可由 Owner 重新明确接受初稿；本次接受之后的新取消、删除、暂停与提交点保护不变。
- 接受期间固定 draft_id，忙碌时保留确认反馈；成功后右侧切换正式结果，不跳回原文件。
  没有放宽 QA、取消/删除门或把用户接受冒充系统验证通过。
- 回归 **77 passed**：初稿接受/预览 18 项（含附件、真实 Manager 停止与后台维护并发、
  同一交付重放），快照/Publisher/账号运行与发布/来源并发相邻回归 59 项。
  保留既有 httpx 弃用警告。Browser 插件不可用，使用既有 Playwright，**4 passed**：
  自动预览、独立下载、换稿确认、正式正文、拒绝不伪成功，以及 1440/390 宽度下文件切换和返回。
  页面身份、无错误遮罩、控制台、桌面/窄屏截图与交互检查通过。
- TypeScript、生产构建通过（既有 chunk 大小提示）；8088 健康和新版静态资源 HTTP 200。
  Standards/Spec 审查发现的接受后退回原件问题已修复、补测并复核通过。
  仅合成案例验证，未代用户接受历史真实初稿、重跑模型、迁移、提交、推送或关机。

## 最新运行核验：2026-09-14 本机服务恢复

- 启动失败已复现：SQLite 热回滚日志使只读 Schema 检查触发
  `attempt to write a readonly database`，后端以退出码 3 循环重启。
  事务最初为何中断尚未证明；未修改只读检查或放宽迁移边界。
- 经用户授权暂停本项目后端监督进程，备份原库及日志到
  `data/backups/before-journal-recovery-20260914-223757/`，两文件备份 SHA256 均匹配。
  由 SQLite 标准读写打开执行恢复，未手动删除日志或修改业务状态。
- 恢复后完整性检查为 `ok`、外键问题 0，Schema 预检通过；任务共 28 条：
  completed 21、failed 4、cancelled 1、candidate_ready 1、needs_input 1。
- 仅重启本项目后端，8088 健康检查多次通过，观察期间未再次重启；浏览器确认
  `/data-prep` 与 `/` 可加载登录界面，无页面异常。未登录执行任务或调用真实模型。
  启动输出保存于 `logs/backend-recovery-20260914.*.log`；备份与日志仅留本机。

## 最新本地核验：2026-09-14 初稿先行与用户接受交付

- Pi 主执行在输出文件集合完整且可重开后冻结初稿，界面提前显示预览/下载。快照绑定
  Owner、TaskRevision、Run、SHA；半成品 Office/PDF 解析错误只延迟展示，不中断主执行。
- 文档覆盖及通用候选检查共用无进展预算：连续两轮无新增证据、已通过检查或缺口改善，
  停止重复补救，确认静默后转为待决定；不按措辞、缓存次数、顺序或解析器版本冒充进展。
- 用户接受精确初稿后新建修订并复用唯一 Publisher。未验证状态及接受记录进入 Manifest，
  不冒充独立验证通过。确认框固定 draft_id；后台换稿不替换待接受对象。没有用户确认不接受。
- 账号、Owner、冻结来源、取消代数、删除、P0、文件重开/哈希/格式集合仍强制校验。
  接受与自动完成竞争、提交前取消、发布中断重放均有隔离回归；不生成第二个执行 Run。
  后续修订从原运行继承模型配置，但新外发仍独立授权。初稿读取使用既有来源删除保护。
- 回归去重计 **183 passed, 1 skipped**：初稿/接受/API/发布/进展 41 项，Runtime 71 项，
  工作台与来源读取/删除 71 项；跳过的是未启用真实 Docker 的既有测试。浏览器 **2 passed**，
  覆盖先展示/下载、取消确认、确认期间换稿、接受冲突不伪报完成；已查看真实渲染截图。
  TypeScript 与生产构建通过，保留既有大 chunk 提示；HTTP 测试有既有 httpx 弃用提示，未安装依赖。
- Standards/Spec 两路只读审查发现的半成品中断、换稿、删除保护缺口均已修复并复核通过。
  无生产迁移、真实模型调用、实际提速测量或真实用户资料验收；当前 8088 未监听，浏览器使用
  隔离前端预览。未提交/推送；不重复执行上轮 Git/关机。
- 边界：本轮不降低 OCR 置信度门槛，不宣称定位/正文置信度分层已完成。Legacy/CoreMind
  没有初稿事件时不伪造初稿；目前初稿完整文件集合上限 128 MiB，文本预览前 16000 字。

## 最新修复核验：2026-09-14 文档超时与进度

- 已复现并修复 PDFium 并发调用：校验、预览渲染与发现页渲染共用进程内短时互斥；
  OCR 等待不占用该锁，来源读使用及删除保护不变。未新增依赖。
- 文档读取异常转为脱敏的 `DOCUMENT_RETRIEVAL_FAILED`，原始异常仅写服务日志。
  连续三次文档工具失败即停止，包括发现接口正常返回但整批页面均未知的情况；
  Shell 成功不能清零失败预算。显式 sleep 等待被拦截，单个 Shell 命令最多 300 秒。
- 序数任务默认每批发现五页，返回下一批定位；显式指定页范围仍受冻结授权约束，
  不将第 N 份等同第 N 页。继续复用既有 Owner 隔离证据缓存，不降低可信读取要求。
- 完整性检查仅在明确 `passed=true` 时呈现完成，失败或缺少通过结论不能显示完成。
- Standards/Spec 审查发现的整批未知重试漏洞已补红绿回归并复核通过。
  最终受影响组合回归 `125 passed, 1 skipped`（未启用真实 Docker 测试）；
  进度投影与候选覆盖回归另 `26 passed`，合计 `151 passed, 1 skipped`。
  扩展等待预算测试在本机 Node/TypeScript 环境实际执行；纯 Python 环境明确跳过，
  尚未新增 CI 必跑门。本轮没有真实模型/真实 PDF 交付验收，也未测量真实提速比例。

## 最新修复核验：2026-09-13 来源并发读取

本节为本次上传的最新修复事实；下方旧整改路线不因此恢复。最新授权与接手步骤见 `handoff.md`。
来源预览与执行共用独占锁造成 `source_in_use`，现改为短锁登记持久读使用，冻结、预览、
Pi/CoreMind 读取可并行；删除仍等待全部读者收口，未知使用、Owner、SHA 与删除意图保护保留。
新增并发回归 5 项、既有资料复用/运行读取/删除回归 51 项通过，两路静态审查无必修项。
证据限合成资料、隔离数据库、实际接口与来源读取代码，不代表用户真实 PDF 或模型交付已验收。
本次只上传修复与交接，其他本机工作台改动不在提交内；推送成功后按用户要求正常关机。

> status: `OLD_REMEDIATION_STOPPED / HUMAN_AUDIT_NEXT / P1_NOT_COMPLETE`
>
> last_verified: 2026-09-12 America/Los_Angeles（用户结束旧 R0–R6 整改，准备人工全面审核；
> #138/#143/#144/#145 为 `CLOSED / NOT_PLANNED`，不是完成；远端
> `main@ec4cf0593e7e858bb0ddb5b8a47af2fc9ed3302c`）
>
> authoritative_branch: `main`
>
> p0_engineering_baseline: `453e900837109be18da47985db997e58863fbf30`
>
> identity_rule: 公开身份以 GitHub `main` 与本文件所在提交现场读取结果为准

当前停点以根目录 `handoff.md` 和 GitHub 现场为准。下方 0.1 的旧站点诊断保留为历史证据，
不是恢复 #138 或继续旧整改的指令。`ENGINEERING_VERIFIED`、`LIVE_ACCEPTED`、
`REMOTE_ENFORCED` 和 `RELEASED` 是不同证据等级，不能互相替代。

## 0. 当前 P1 状态

### 2026-09-13 本地人工审核中的新授权：对话工作台

用户在人工审核中逐项要求保留全局导航、增加侧栏收起、简化首屏、上传后右侧预览，随后
明确要求继续接通无附件对话。这是本地新授权，不恢复旧 R0–R6、#138 或 P2。

- 本地实现：首屏和文件预览已调整；`/api/semantic-workspace/draft/turns` 复用模型连接授权与
  语义理解，支持无附件多轮讨论。普通讨论不触发采集；明确的在线采集执行请求接回既有
  Conductor 采集、清洗、分析、质检和报告链路，不再因没有附件而分流为纯聊天。
- 任务衔接：上传文件并发送后，完整对话与当前原话进入既有任务创建链路；不自动读取其他
  任务或记忆。统一输入框明示本次数据去向，点击发送或按 Enter 确认本次外发，不保存永久
  授权；发送占位防止未知响应自动重发。
- 模型选择：已配置的个人与平台模型合并为一个下拉菜单，点选即生效；统一输入框不再展示
  连接配置、重复确认勾选和添加模型面板。禁用或失效的模型不能发送，后台授权检查保持不变。
- 草稿边界：未发送输入保存在当前浏览器的 Owner 草稿；成功回复现已写入当前账号会话，
  新消息返回会话及消息身份，后续追问使用服务端原会话，不是正式交付。
  停止等待不表示外部模型已停止计费；采集任务复用既有会话持久化，当前工作台展示进度、
  报告下载、刷新恢复和停止执行。该兼容采集结果不是新语义工作台的正式 Delivery，通用暂停仍未接通。
- 工程证据：后台对话与既有追问回归 `33 passed`；8088 构建页面使用模拟接口的浏览器
  检查 `8 passed`，覆盖多轮、刷新、附加文件、停止等待、外发确认、导航和移动输入。
  本地模型文本协议及外部连接协议均使用合成响应验证；没有真实模型或真实任务交付验收。
- 模型选择简化后再次构建通过，开发页模拟接口回归 `8 passed`，包含桌面/手机点选后直接
  发送、文件任务绑定、无附件多轮、停止等待、明暗主题、上传预览与全局导航；未跑完整端到端套件。
- 采集回归修复：后台相关回归 `36 passed`；新增隔离完整 Conductor 图验证，真实经过连接
  Broker、任务执行、采集适配器、分析、报告文件和 Owner 下载检查；Provider 与外部采集返回
  合成数据。所选连接贯穿编排，模型失败阻止后续产出且不静默换模型；浏览器相关回归
  `10 passed`（含采集报告展示、执行中刷新恢复/停止、不覆盖新输入）。尚未进行真实小红书采集验收。
- 本轮未新增依赖、未迁移数据库、未切分支、未提交或推送；8088 后端健康且已加载新接口。
- 新建任务残留修复：显式重置当前输入缓存与组件身份，仅保留模型选择；旧回复、上传和创建
  结果迟到不覆盖新草稿，服务端历史任务和原文件不删除。先复现旧回复残留，再通过前端相关
  回归 `14 passed`、8088 新构建针对性复验 `4 passed`（模拟接口），类型检查与构建通过。
- 执行过程与历史衔接：既有采集链新增真实阶段开始/完成、资料条数和读取受阻摘要；当前工作
  常显、详细过程可展开，不暴露原始推理或工具日志。完成、失败和取消时随会话保存过程，
  执行中的同进程刷新通过运行状态读取恢复；未承诺服务重启后的在途事件完整回放。
  左侧历史合并当前 Owner 已保存的旧会话，可重新查看过程和报告下载；旧会话没有记录的
  过程不伪造，不转写为语义工作台正式 Delivery。后台相关回归 `73 passed`，前端相关回归
  `15 passed`，8088 新构建复验 `3 passed`；以分段释放的真实 HTTP 流验证“报告返回前已有阶段与条数”，类型检查和构建通过。
  Provider/采集数据均为合成边界，真实站点资格未扩大。
- 历史续聊与消息操作：补回历史会话输入框、模型选择和附件入口；普通追问基于服务端已保存
  的本会话回答，明确新增采集要求继续走原会话执行链。附件任务携带该会话正文进入既有
  文件任务创建，不清除其他新任务草稿；切换会话后迟到结果不抢回导航。
  当前对话与历史回复均提供复制、点赞/点踩与取消反馈、时间戳和 Token；反馈沿用原接口，
  消息响应补回数据库消息编号。连接模型和本地语义调用接入既有用量统计；缺失字段显示未知，
  无记录的旧用量不补算、不显示为零。普通对话也持久化，不再仅是浏览器中的临时答案。
  后台相关回归 `76 passed`；8088 构建页面模拟接口回归 `17 passed`，覆盖复制内容、反馈刷新
  保存、同会话追问及刷新、实时进度、桌面/手机、模型和附件边界；类型检查与构建通过。
  仍未调用真实模型或真实站点，未提交/推送、未迁移数据库、未重启服务；这是工程验证，待用户人工验收。
- 人工复核补漏：已完成的旧浏览器草稿原先不再读取服务端消息，导致反馈按钮缺编号而禁用，
  同时漏掉时间戳及用量。现在按本会话消息编号或唯一正文匹配补全，不覆盖未发送输入；
  无法唯一匹配时提示打开历史，不按消息位置猜测。实际检查发现旧回复虽无用量元数据，
  Provider 账本仍有记录；读取历史时按运行身份、或排除新记录后的唯一旧回复/旧运行只读
  对账，显示为“执行阶段”（不含旧版会话外需求识别调用），不改原始账本或历史正文。
  采集过程新增常显阶段与来源卡片，显示来源标题、域名、准备访问/取得资料状态，点击在
  新标签页打开来源；采集器返回结果后即推送，不等待报告。不宣称摘要是全文，不暴露
  URL 鉴权参数或本机地址；历史没有记录的来源不编造。初轮相关后台 50 项通过，账本恢复
  最终定向 13 项通过，8088 最终浏览器回归 19 项通过；类型检查及构建通过。实际旧报告
  账本只读核对为 6 次执行调用、输入 15648/输出 11461/合计 27109（不含需求识别）。
  全部站点/模型测试为隔离合成输入，没有重新执行该真实采集任务。

- Cookie 验证串入正式采集结果修复：用户另行授权本地修复。MediaCrawler 每次调用使用独立
  临时输出目录，验证、不同 Owner 与并发任务不再共享按日期追加的 JSON；结果仅从本次目录
  读取，搜索来源词缺失或不符时拒绝使用。保留七平台目录差异及详情/评论路径；超时与取消
  有界回收本次进程树，随后清理临时产物，不改写历史采集文件或报告。
- 采集核验展示：Checker 分数达标且明确返回布尔 `passed=true` 才通过；补充平台、搜索词和
  实际条数，并要求主题不符/零相关样本失败。未通过结果在页面与导出报告中明确标记，
  不再显示高分良好或建议沉淀模板；语义相关性判断仍依赖模型，未宣称真实站点验收。
- 本轮证据：先复现“验证你好后采集中信私银仍返回你好”及高分覆盖拒绝，再修复；本地合成
  子进程与模型边界回归 `100 passed, 1 skipped`（Windows 符号链接权限不足的既有测试），
  覆盖并发、历史污染、来源不符、七平台目录、详情/评论、超时/取消与继承输出管道回收。
  Standards/Spec 审查发现的回收与平台目录问题已修复，复核无必修项；diff 检查通过。
  既有开发监听器已自动加载最终修改，8088 健康检查通过；未手动重启服务、未调用真实
  站点或模型、未安装依赖、未迁移数据库、未提交或推送。已有错误报告须重新执行任务生成。

- 报告模板入口修复：用户授权补回工作台中缺失的“沉淀为模板”按钮，当前对话与历史报告均
  按当前 Owner 的服务端待确认状态展示；领取沿用既有原子动作，保存中禁用重复点击，失败
  或结果未知不自动重试。历史动作已处理或过期时明确提示，并提供“查看我的模板”入口。
  提炼沿用原任务所选模型及冻结连接版本，绑定失败在写入前停止；确认路径用本地关键词
  去重，不额外调用向量、重排或裁决服务。既有进程内暂存方式未改，服务重启后旧动作需重新
  执行任务产生，不从历史报告擅自恢复授权。
- 模板修复证据：后台相关回归 `87 passed, 1 skipped`（既有 Windows 符号链接权限限制）；
  8088 构建页面模拟接口检查 `7 passed`，覆盖新回复、旧草稿、历史、桌面/手机、单次保存、
  失败及刷新后的状态核对。类型检查和构建通过，页面截图已检查，Standards/Spec 复核无必修项；未调用真实模型或站点，
  未迁移数据库、未安装依赖、未提交或推送，真实提炼与保存仍待用户操作验收。

- 输出格式交互修复：统一输入框默认“输出：自动”，明确要求 JSON 时仅提交 JSON；不再因
  上传文档而强制选择 DOCX/PDF。未指定时按任务目标给出可修改的 XLSX/Markdown 建议。
  格式选择区分自动与手动；兼容旧默认草稿，多格式、更正与排除指令同步到实际提交；
  手动选择冲突时可一键按文字处理。本会话前轮用户明确格式可继承，本轮更正优先，助手
  建议不作为用户授权。上下文预览与输入框复用同一格式解析，后台继续校验并冻结实际格式。
- 格式修复证据：先复现旧 PDF 上传/JSON 要求不一致，再修复；后台创建/冻结及既有格式
  渲染检查 `22 passed`，8088 构建页面 Chrome 隔离接口回归 `26 passed`，含桌面/手机、
  旧草稿、多轮格式继承、反馈、历史、附件与导航。类型检查、构建及差异检查通过，8088
  健康且入口命中本轮构建。初轮测试在后台执行器关闭阶段等待，最终创建接口用例不启动
  执行器，只验证真实创建与读取接口；未把测试关闭等待当作生产运行器修复。
  格式提示采用有界明确指令识别，不宣称覆盖任意自然语言；未执行用户真实 PDF 任务、
  未新增依赖、未迁移数据库、未手动重启服务、未提交或推送。

以下为旧方案终止时的历史状态，不代表上述本地新授权被暂停。

**用户已终止旧整改方案。新计划形成前不继续 R0–R6，不恢复 #138，也不自动启动 P2。**

- 远端仅 #113、#146、#147 OPEN；#113 正文清单已过期，仅作历史地图。
- #139/#140/#141/#142/#159/#186 为 `CLOSED / COMPLETED`。
- #138/#143/#144/#145 为 `CLOSED / NOT_PLANNED`，原验收条件没有完成。
- 旧 Codex Draft PR #176–#181、#184、#188 已关闭并保留历史；6 个 Dependabot PR 待逐项评估。
- 远端无 Tag/Release；当前没有活动实现、版本或部署承诺。

### 0.1 2026-09-11 历史诊断快照（已失效，勿作为执行指令）

- GitHub 现场：#114–#137、#140、#159 已关闭；#138/#139、#141–#145、整改地图 #113 及后期 #146/#147 共10票 OPEN。
- 本地分支 `codex/issue-140-universal-context`；远端 main 为
  `a82b6a4f9838781b97d81efc60ea5e9e01b91f2d`。最后主线合并为 #140 / PR #175。
- **#139 本地主线整合中**：复用现有来源获取/Artifact/任务主链，增加 Owner 绑定的公开 HTTP 与数据库
  只读连接来源；选择哈希已与尝试命名空间解耦，数据库鉴权/权限错误可精确投影。迁移代码头为
  `webui_0019`，生产库仍是 `webui_0018`，未执行生产迁移。
- **#139 当前工程证据**：连接读取与契约 31 项、正式交付 1 项、迁移 87 项、前端构建和浏览器 27 项通过；
  影响面 282 项通过；Draft PR #182 最终 SHA `35bf3d7` 的 minimum-ci run `34576324388`
  三项全绿。真实外部 HTTP 与真实数据库凭据未提供，故不记为 `LIVE_ACCEPTED`。
- **#140 工程完成并关闭**：PR #175 补既有模板库适配、个人记忆并发纠正、
  恶意控制指令拒绝和旧 TaskRevision 不回写；最终 SHA `1956ab9` 的 minimum-ci run
  `34584692766` 三项全绿，本地后端上下文 22 项、CI 合同 12 项、前端旅程 14 项、完整浏览器
  388 项及构建通过；unified-workspace run `34584966294` 两轴全绿；已 squash 合并为 `a82b6a4f` 并关闭 Issue。真实语义/运行实例和用户视觉验收尚未完成，不记为 `LIVE_ACCEPTED`。
- #138 候选主要位于被忽略的 `.artifacts/issue-138/`，不能假定新 clone 有这些文件；正式产品未切换。
- **暂停前最后验证**：XHS 诊断采样边界在一次真实 `start` 中得到 `http=200`、
  `status=observed`、`phase=done`、`error_type=null`、`sampling_truncated=true`；会话关闭、
  容器/网络零残留、15 项保护未变。但 `visible_search_inputs=0`、查询未提交，不能冒充取数成功。
- **后续独立 Cookie 优化尚未验收**：另开任务后再核对管理员/用户输入接线、
  Vault/Owner/站点/账号隔离、失效与网络未知语义、只读操作门；不得算入 #138 完成度。
- 通用登录状态/Owner隔离、原 attempt/Run 前置认证续行、二维码私有读取与取消已有隔离工程证据。
  浏览器生命周期最新37项回归和串行 Spec/Standards 通过；真实容器跨进程恢复/部分清理重试通过，
  但该容器探针用合成健康 Worker，不代表网站登录。
- **网络层已解决并经真实运行证实**：宿主存在 Mihomo 虚拟网卡（DNS `198.18.0.2`，FakeIP 来源）。
  容器内对照实测：两台本机配置的 LAN DNS **4/4 可达**（约 400ms，返回公网地址），
  公共 DNS `223.5.5.5`/`119.29.29.29` **0/4 被阻断**。诊断代理固定用 LAN DNS 后，
  三个已准 CDN 域由 `benchmark_198_18_15` 变为 `global`，容量类 HTTP429 消失。
  **公共 DNS 在容器内不可用，不得改用**；宿主全局 DNS 与私网防护未改。
- **观测链路已可靠**：原 `diagnostic_worker` 只把 `goto` 包在 try 内，超时即退化成
  `observation_unknown`；现改为全阶段保留 `phase`/`error_type`/已收集事实，采样有界。
  该修复已在一次真实 `goto` 失败中验证（精确给出 `phase='goto'`、`error_type='Error'`）。
- **登录面板判据已取得真实 DOM 证据**：面板是**普通 DIV**，无 `role`、无 `aria-modal`。
  旧 `_login_panels()` 的三条件（`role=dialog`、`aria-modal='true'`、精确 `扫码登录`）
  在四次测试观测中**全部返回 0**，已被证伪。真实可见标记为 `手机号登录` 与
  `可用 小红书 或 微信 扫码`；判据应为两者可见同现并取其共同祖先
  （两次独立观测互证：depth=2、bbox `(240,120,800×480)`）。
  **该改写已落成产品候选 `site-adaptation/candidate/browser_worker.py`
  并在真实站点确认通过**：`candidate_login_panels` 由旧合同的恒为 0 变为 **1**，
  `panel_bbox=(240,120,800,480)` 与互证值逐位一致。候选**仅改** `_login_panels` 一个方法，
  取码选择器一字未动；**尚未接入产品 `src/`**，未做双轴审查与提交。
- **搜索框一直存在，是选择器错了**：真实搜索框为 `INPUT type=text`、
  位置 `(400.8, 16)`、尺寸 478.4×40，`role` 与 `aria-label` 为空且 placeholder 不含“搜索”，
  因此现有四选择器全部落空。已取到稳定结构标识 **`id="search-input"`、`class=["search-input"]`**，
  且 placeholder 哈希跨两次独立观测逐位相同（`6c6b92b5…`），选择器 `input#search-input` 可用。
  注意：该函数只在**诊断** worker 里，产品 Worker 的 `search()` 是直接 `goto(search_url)`。
  **产品侧未接入。**
- **【部分失效，不得直接复用】~~"二维码未渲染，原因已定性为范围门"~~**：
  该判断建立在"面板内 `160×160` 容器 `children: []`"之上，而**面板作用域本身可能有假** ——
  B 站就是同一个毛病（见下文 B 站面板作用域条目）。**此判断未经复核。**
  当时确证的部分仍成立：只读端点形态识别发现被拒请求中**存在一条同时含 `qrcode` 与
  `login` 的 `edith.xiaohongshu.com` `/api/` POST**（另有含 `captcha` 一条），
  该域在冻结策略里**只放行 `/api/sns/web/v1/user/selfinfo`**。
  该域**已按用户授权窄放宽**（`scope-login-narrow`）：放宽后含二维码/验证码关键词的
  被拒归零，**但错误页依旧** —— 故"范围门是唯一原因"**不成立**。
  取码选择器因无证据仍一字未改。
  **要重开此判断，先按 B 站的办法复核 XHS 的面板作用域。**
- **匿名搜索路径不可用**：走产品 `search()` 的直接 `goto` 分叉实测，
  `source_allowed=true`、`http=200`，但 **`identity_count=0`**、`login_panels=0`、
  `visible_ui_markers=0`，被拒请求仅 6 条（首页那轮 80+）——页面几乎是空的。
  与既有记录"搜索页 HTTP200 仍只有骨架"独立一致。
  未排除项：采样只在 `domcontentloaded` 后等 1500ms，SPA 渲染可能更慢。
- **B 站首批结构证据（面板仍未知）**：匿名首页 `http=200`、`location=/` 根路径，
  可见标记 **`登录`=1、`立即登录`=1**，但 `panel_found=False`
  —— **B 站不自动弹面板**，与小红书不同，需点 `登录` 才出现；该动作本片事先排除。
  搜索框取得稳定类名 **`class="nav-search-input"`**（`(493,23) 279×20`，placeholder 长度 0）。
  被拒以 `api.bilibili.com` 12 条为主（该域只放行 `/x/web-interface/nav`）。
- **范围已按用户明确授权窄放宽一处**：`edith.xiaohongshu.com` 上**路径含
  `qrcode`/`captcha`/`login` 之一**（长度 ≤256）的接口开 POST 例外，仅该域、仅该站；
  其余域名/路径/方法一律不变（含 18 条对照断言证明差异只发生在这些路径上）。
  放宽后 `edith` 被拒由 10 条降到 7 条，**含二维码/验证码关键词的被拒归零** —— 策略层确认生效。
  候选在 `scope-login-narrow/candidate/site_policy.py`，**尚未接入产品**。
- **第二处放宽（登录页导航）已按用户授权落地，但落地页是错误页**。
  放宽本身写在两处：`request_allowed` 与 `SessionScope.allowed` 的**导航门**
  ——第一次只改了前者，导航仍被拒（该门要求导航必须命中冻结搜索页），
  这是**同一处授权改动没落到执行层**，不是新的放宽；补齐后
  `被拒导航` 由 1 条降为 **0 条**、`scanned` 由 2 升到 45、`location` 即登录页路径。
  但落地页整页唯一的近方形是 **`error-img-box` / `error-img`**，零登录标记、零输入元素
  ——**站点把匿名会话导向了错误页**。成因未证实。
- **匿名搜索是稳定不可用，不是偶发**：跨 session 度量（样本量事先固定 N=6，
  每个 session 独立 fixture、只做一次只读分类）结果 **6/6 逐项相同**
  （`http=200`、`source_allowed=true`、`identity_count=0`、`denied=6`），**零分裂**。
  搜索页是**稳定可复现的空壳**。注意：波动只出现在**首页**，不是站点整体不可复现。
- **【已推翻】~~B 站二维码"按 session 出现"~~** ——那是**观测缺口**，不是站点行为。
  `bilibili-qr-scan` 那次"看见码"只是因为 `panel_bbox: null` 回落到 `document` 作用域；
  **面板一探到就看不见**。真因见下条。
- **B 站面板作用域已修复并实测通过（`bilibili-panel-scope-fix`）**：锚点 `密码登录` 是弹窗
  **右半边**的页签、提示词 `注册` 也在右半边表单里，所以"从锚点向上找含提示词的最近祖先"
  **必然停在右半边** `bili-mini-login-right-wp`（`592.5,180,400×293`），把左半边的码切在作用域外。
  三条独立事实**在改动前就已在冻结证据里**：12 个 session 的 `bilibili_generate` 全 200；
  右半边近方形 0 而弹窗容器 `bili-mini-content-wp` 含可见 `IMG 160×160`；
  唯一看见码的那次正是面板探测失败回落到 `document`。
  修复：B 站面板改判为"**含可见近方形图形（60–320px，宽高差 <12）的最近祖先**"，
  用的全是几何，**不认类名、不认 id**；小红书侧判据逐字未动。
  **实测（N=4，判据跑前写死）**：`near_square_count` **4/4**（4/4/8/4）；
  `panel_depth` 由 2 变 **3**、`panel_walk_extended: 1`；`panel_bbox` 变 `230,130,820×460`
  （3/4 —— session 3 是 `entry_missing` 没渲染登录入口，**不是作用域失败**）。
  二维码本体：`DIV.login-scan-hover-wp 173×173 → DIV.login-scan-box → DIV 160×160 →
  IMG 160×160 visible has_src=true`。
  冻结策略本就放行 B 站二维码接口，**B 站阻塞与范围门无关**。
- **`qr_ancestry` 仍 0/4，是缺口不是结论**：已知它只取 `img[src]` **前 40 个可见节点**，
  而同一时刻码确实在 DOM 里且可见。**探针没找到它，原因未验**，只登记为缺口，
  不得据此说站点如何。教训：**不要把"探针没看见"当成"站点没给"**——本会话已犯两次
  （VPN 假设、B 站二维码），两次都靠对照/回看冻结证据推翻。
- **按站拆分 `_login_panels` 是已知缺口**：`site-adaptation` 的新判据用的是小红书文案，
  对 B 站恒返回 0。旧合同虽已被证伪但至少站点无关。
  **不得**为让 B 站返回 1 而放宽判据，也**不得**为凑取码而把范围放宽到整页。
- B 站搜索框取得稳定类名 `class="nav-search-input"`（`(493,23) 279×20`，placeholder 长度 0）。
- **站点侧按 session 不可复现（当前最大障碍）**：同一 session 内连续 5 次加载完全一致
  （`panel_present:0`、`with_inputs:5`、`with_includes_error:5`、`goto_failed:0`）；
  跨 session 则六次里只有两次出现登录面板。每次都用全新 `new_context`（无 `storage_state`），
  差异只能来自站点侧按 IP/频次/时段的抽样或分级。**成因未确定。**
  站点自身 `TypeError: i.includes is not a function` 在好坏两种渲染下都存在，已排除为波动原因。
- 未出现面板的稳定态下整页**仍有且仅有 1 个可见可编辑输入**（5/5 一致），
  疑似顶部搜索框，但本片只计数未取属性，**不据此宣称匿名路径可行**。
- **不得反复开 session 去“碰运气”取面板**：那是用重试掩盖不可复现性。
  若要度量出现率，必须先显式设计样本量与记录方式。
- #138 仍缺两站真实扫码、身份验证、加密保存复用、失效重登和原任务续行，以及独立 main 整合/最终CI/验收。
  原 PR #181 为堆叠草稿，不能把 #139/#140/#141 一并合入来单独关闭 #138。
- #139 已独立于堆叠草稿进入 Draft PR #182；#140 已在独立 Draft PR #175 修复；#141–#145 候选仍冻结。#142 HTTP取消/关闭仍有未验证缺口；#144四包安全升级只在隔离候选验证，
  不代表运行依赖已切换；#145退役仍须替代能力和黄金任务验收通过后实施。
- 产品体验已确认：query、模型选择、文件导入与对话反馈为主；智能体自动处理后台步骤，文件自动右侧预览；
  保留复制、点赞/踩、分享。旧4192配置向导被否定，不再作为验收目标。
- 模型配置 #159 已关闭；八家目录与本地模型入口有工程证据，百炼/DeepSeek/本地Qwen三款文本连接有受控实证，
  不等于所有目录模型、工具调用或生产配置均获资格。
- 启动修复历史证据：本机 WebUI 已由0009显式迁至0018并修复本机JWT启动缺口；恢复点与
  `.artifacts/issue-138/startup-repair/final-startup.json` 保留。后续候选0019及更高迁移未应用原库。
  本次仅核对8088/5173仍监听，未重启或重新执行原库验收。

以下 P1-01/P0 章节是已注明时间的历史证据，不作为当前待执行任务。

### 0.2 上轮 P1-01 工程收口证据

- P1-01 的决策、规格、原型与实现工单 #83～#98 已全部关闭；上轮于 2026-09-04 现场查询 GitHub
  Open Issues 为 0。关闭 #81 只表示 P1 决策地图与首个纵切片完成，不代表 P1-02～P1-05 已实现。
- CoreMind 0.7.1 已通过锁定制品接入。PR #107 合入
  `main@12170eebf1d2f4bad5c86c2a6c38d0bef0a4f998`，Issue #97 已关闭；精确源码、协议、Worker、
  wheel 与 digest 见 [PR #107](https://github.com/Eclipseic1848/Mangrove_ai/pull/107)，架构原型见
  [原型报告](../plans/2026-08-27-p1-coremind-agentkernel-prototype-report.md)。本任务没有修改本机
  CoreMind 仓库，也没有发布制品。
- #98 由 PR #110 合入 `main@5adeacf3aecbd55bd5fe771a35d25a4caa195af3` 并关闭。它新增统一
  收口测试、启动前数据库迁移只读预检与缺失的 PDF 开发测试依赖，并移除固定旧局域网 IP、
  直接访问已被 AgentKernel 取代的 `_pi_runtime` 两条过期测试假设。
- 后端固定种子全量回归为 `2352 passed, 13 skipped, 1 deselected`；唯一 deselect 是被其他任务
  修改且受保护的 G1 freeze 自校验。Issue #98 相关后端组合为 `235 passed, 1 skipped`。
- 前端正式构建通过；完整 Playwright 为 `77 passed`，其中统一数据工作台 40 项，含 light/dark
  axe 检查。固定 Pi/CoreMind 黄金对照为 `1 passed`，覆盖两种 Runtime 的同场景运行中取消、
  无迟到事件与资源清理。
- `start_all.bat --no-pause` 实际返回 0；8088、5173、8080、3002、1200 均监听，8088 与 5173
  返回 HTTP 200，`/api/health` 返回后端健康。脚本继续是本机忽略文件；可提交的只读迁移预检
  位于 `scripts/check_dev_database_migrations.py`。
- UTF-8 检查通过 1249 个文件，`git diff --check` 通过；Standards + Spec 双轴终审均无问题。
  PR #110 的 minimum-ci run `33925622082` 中 `backend-fast`、`frontend-build`、`secret-scan`
  全部成功。
- 上述结论仅为 `ENGINEERING_VERIFIED`。Issue #98 没有执行真实外部网页/Provider 用户旅程、
  新的本机生产迁移、用户验收、部署、Tag、GitHub Release 或包发布；2026-08-26 的历史本机
  生产迁移已完成，本工单未授权的是新增迁移、恢复覆盖、备份处置与 Key/Secret 轮换。
  这些证据等级不得互相替代。
- Agent-Reach 调研继续作为有效候选知识源；不整包安装，不自动启用 OpenCLI/xiaohongshu-mcp。
  认证来源未来仍须按 Owner 隔离 Cookie/SecretRef，明确失效时暂停同一 Run 并由该 Owner 扫码。
- 已清理可再生旧产物：`.scratch/**`、`test-results/**`、`frontend/premium-audit.json`，共约
  118 MB，进入 Windows 回收站；源代码、测试、ADR、规格、正式证据、数据库、备份与他人改动未删。

### 0.3 上轮可复现命令

以下命令均从仓库根目录执行并返回 exit code 0；前端命令先进入 `frontend/`。完整后端仅排除一项
被其他任务修改的 G1 freeze 自校验。

```powershell
.\.artifacts\ci-clean-venv\Scripts\python.exe -X utf8 -m pytest -q tests/test_source_acquisition.py tests/test_source_acquisition_api.py tests/test_web_source_delivery_api.py tests/test_work_trace.py tests/test_agent_kernel.py tests/test_coremind_agent_kernel_adapter.py tests/test_pi_runtime_workspace_api.py tests/test_semantic_workspace_api.py tests/test_database_migrations.py tests/test_dev_database_preflight.py tests/test_issue98_workbench_closeout.py
.\.artifacts\ci-clean-venv\Scripts\python.exe -X utf8 -m pytest -q --randomly-seed=0 --deselect tests/test_g1_independent_runner.py::test_independent_g1_dry_run_verifies_frozen_blind_set
Push-Location frontend
npm run build
npx playwright test --workers=1
Pop-Location
$env:MANGROVE_RUNTIME_ADAPTER_GOLDEN_TEST='1'; $env:PYTHONPATH=(Resolve-Path '.\.venv-coremind-host-verification\Lib\site-packages').Path; .\.artifacts\ci-clean-venv\Scripts\python.exe -X utf8 -m pytest -q tests\test_runtime_adapter_golden.py
.\.artifacts\ci-clean-venv\Scripts\python.exe -X utf8 scripts\ci\check_utf8.py
git diff --check 12170eebf1d2f4bad5c86c2a6c38d0bef0a4f998..HEAD
cmd.exe /d /c start_all.bat --no-pause
```

端口证据：8088、5173、8080、3002、1200 均处于 Listen；8088/5173 HTTP 200；
`http://127.0.0.1:8088/api/health` 返回 `{"ok":true,"service":"mangrove-webui"}`。

## 1. 当前公开身份

- 公开仓库 `Eclipseic1848/Mangrove_ai`；默认分支 `main`。
- 2026-09-11 现场远端 main 为 `a82b6a4f9838781b97d81efc60ea5e9e01b91f2d`；
  本地分支 `codex/issue-140-universal-context`，有必须保留的未提交用户修改及 #138 暂停候选。
- 本次 `git ls-remote origin refs/heads/main refs/tags/*` 未返回远端 Tag，`gh release list` 为空；
  本地存在历史 `v0.0.4`，不能将其表述为当前远端发布。`v0.0.8` 也未发布。
- 现场10张 Open Issue；PR #175 已合并，PR #176–#182 中 #182 等仍为草稿，另有 Dependabot PR，不自动合并。
- P0/P1-01公共入口同步有历史证据；本次未修改GitHub About、法律/安全文件或远端配置。

## 2. 产品与稳定边界

Mangrove 是统一数据任务平台。用户从文件或其他来源创建不可变 TaskRevision，系统冻结 Owner、
来源、连接版本、外发确认和能力身份，经 Runtime 形成 Candidate，再由独立 Verifier 验证；只有
完整性与 QA 通过并进入 `delivery_published` 的 `output_id` 才是正式 Delivery。

- `8088` 是统一产品入口；`5173` 只用于前端开发。
- `/data-prep` 是统一工作台；按 ADR-0036 不把未上线历史任务/Delivery 的迁移作为新平台完成门，
  但未经授权不删除真实数据，移除旧入口前须证明对应用户能力仍可用。
- Candidate、验证通过、`eligible_for_delivery`、中间 AST 或 Parquet 都不是正式交付。
- 普通用户、管理员、超级管理员是产品角色；“高级用户”不是权限角色。
- TaskRevision、来源快照、Owner、Run、CandidateSet、连接版本、外发确认、Ruleset/能力 digest
  必须冻结并失败关闭。
- Mangrove 自有 Schema 只经 `src/database_migrations` 显式迁移；应用和 Repository 只验证版本。
  LangGraph checkpoint 和用户连接器数据库不属于该迁移体系。
- 25 个 `runtime_config secret=True` 键只允许业务表保存 Owner/配置键绑定的 SecretRef；原值由
  共享 Vault 密文边界持有。本机生产迁移已于 2026-08-26 显式完成；旧备份处置和 Key/Secret
  轮换仍是独立人工门。

## 3. 当前能力矩阵

| 能力 | 当前证据等级 | 当前边界或尚缺 |
|---|---|---|
| 公域采集与 Conductor | 可用 | 企业 API、业务系统、对象存储和统一生产 Adapter 尚未完成 |
| 数据工作台 `/data-prep` | 可用 | 历史任务与 Legacy Delivery 仍保留兼容路径 |
| P1-01 匿名网页统一工作台 | `ENGINEERING_VERIFIED / CLOSED` | 受控工程闭环已验；真实外部网页、Provider 与用户体验未验收 |
| HTTP/数据库只读连接来源 #139 | `ENGINEERING_VERIFIED / CLOSED` | PR #182 已合入；真实生产数据库、迁移、部署和外部来源资格仍独立 |
| 三类来源模板与个人记忆 #140 | `ENGINEERING_VERIFIED / CLOSED` | 既有模板适配、Owner记忆纠正、冻结链及同提交CI已通过；真实语义/运行实例和用户视觉验收未执行 |
| 自动任务与反馈 #141 | `ENGINEERING_VERIFIED / CLOSED` | PR #183 已合入；真实 Provider、生产和部署仍独立 |
| 统一页面体验 #142 | `ENGINEERING_VERIFIED / CLOSED` | PR #185 已合入；本机运行前端是否加载最新构建须现场核对 |
| 手填 Cookie 稳定路径 #186 | `ENGINEERING_VERIFIED / CLOSED` | PR #187 已合入；不等于 #138 扫码路线或真实站点资格 |
| vNext 默认正式 Delivery | `LIVE_ACCEPTED` | 默认/Pi/Legacy、P0 回滚与 Owner 隔离已验收；不代表稳定 Release |
| Candidate 同 Run 重验 | `LIVE_ACCEPTED` | 追加式 Attempt、独立 Provider 授权、精确 Attempt 发布；未知结果不自动重试 |
| 11 种交付预览 | `ENGINEERING_VERIFIED` | 不等于每种格式均有生产用户验收 |
| 覆盖感知文档检索 | 代表任务验证 | 不等于所有文档类型和部署环境资格 |
| 多模型连接与 Provider 安全门 | 工程验证 + 受控真实资格 | 新 Provider、真实业务外发和费用仍逐次授权 |
| Agentic Capability #9～#17 | 已完成并关闭 | admin_gray 治理纵切面完成；普通用户能力市场、远程 MCP/Registry 未开放 |
| G1/G2/G3/G4 与 G5 本机门 | 已取得正式或真实证据 | 目标 Linux/GPU 服务器、长期容量与灾难恢复仍待部署时验收 |
| 显式数据库迁移 #56 | `LOCAL_PRODUCTION_MIGRATED / CLOSED` | 体系已完成；本机 WebUI 后续已显式迁至 `webui_0018`，Scheduler 为 `scheduler_0001`，当前启动修复见第 0 节 |
| SecretRef 统一 #57 | `LOCAL_PRODUCTION_MIGRATED / CLOSED` | 高敏副本演练后，本机生产 `webui_0004` 已真实应用；原 Vault key 保留，未轮换或销毁 |
| 依赖与漏洞治理 #58 | `ENGINEERING_VERIFIED / CLOSED` | 历史固定制品扫描通过；不代表当前仓库零漏洞。新增依赖告警待诊断，Node 限期风险按 2026-09-25 复查 |
| 远端 CI 与主分支保护 | `REMOTE_ENFORCED` | Ruleset `21624053` 强制 PR、strict 三项 CI、讨论解决和禁止强推；单维护者审批数为 0且无 bypass |

## 4. P0 #54～#60 收口矩阵

| Issue | 目标 | 当前事实 | 尚缺的完成证据 |
|---|---|---|---|
| #54 P0-01 | vNext 默认链路与真实普通用户闭环 | `CLOSED / LIVE_ACCEPTED`；实现基线 `20d3a2a9` | 无；保留历史证据，不重复执行 |
| #55 P0-04A | 最小 CI 工程门 | `CLOSED / REMOTE_CI_VERIFIED`；运行 `32947317082` 三项全绿 | 无；required checks 由 #59 落地 |
| #56 P0-05 | 显式数据库迁移体系 | `CLOSED / LOCAL_PRODUCTION_MIGRATED`；PR #76 合入 `453e9008`，副本演练后于 2026-08-26 显式迁移本机生产 WebUI 与 Scheduler | 无；恢复覆盖仍须重新授权 |
| #57 P0-02 | 配置中心 SecretRef 统一 | `CLOSED / LOCAL_PRODUCTION_MIGRATED`；PR #76 合入 `453e9008`，生产 `webui_0004` 已应用，原 key 保留 | 无；Secret/key 轮换和备份处置仍是独立人工门 |
| #58 P0-03 | 依赖拆分与漏洞治理 | `CLOSED / ENGINEERING_VERIFIED`；PR #76 合入 `453e9008`，五组 clean install、真实 Linux 镜像、完整核心回归、最终审查和风险记录完成 | 无；Node 限期风险仍须复查 |
| #59 P0-04B | `main` 分支保护 | `CLOSED / REMOTE_ENFORCED`；用户确认单维护者模式，Ruleset `21624053` active；PR #78 证明失败阻断、修复后可合并且未合并探针 | 无；未来有第二维护者时另开权限变更把审批数从 0 提升为 1 |
| #60 P0-06 | 当前状态与交接收口 | `CLOSED`；PR #79 普通合入 `main@a2f13e3d`，合并后 CI 33041624585 三项全绿；公共/权威文档与 Security 已同步，Code of Conduct、MIT License、About 无需变化 | 无；最终状态只从本文件所在提交和 GitHub #60 现场读取 |

## 5. 生产差异与风险

### 5.1 数据库与 Secret

- 以下为 2026-08-26 P0 历史迁移证据；当前 Schema 与启动修复以第 0 节、0.1 节和 PR #110 为准。
- #56 已在生产 `data/webui.db` 的只读一致性副本上应用 `webui_0001..0003`：原 74 张表、
  10,216 行历史数据无意外改写，重放零 revision 且字节不变，恢复副本与备份逐字节一致；真实
  合法 `vnext_default` 缺口已修复并回归。生产原库 SHA、大小和 mtime 全程未变。
- #57 高敏生产副本演练已完成：生产库与匹配 key 在 8088 无监听、源库静止时复制到受限目录；
  副本从 legacy 迁至 `webui_0004`，实际 1 条配置 Secret 形成 1 个 opaque ref 与 1 条密文，
  孤儿密文为 0，受信读取、恢复点重放、完整性和外键验证通过。
- 迁移副本、迁移后新备份和恢复点重放副本的明文命中均为 0；现场不存在 WAL/journal/shm，
  因而没有可扫描 sidecar。迁移前恢复点按预期命中 1 项旧明文，仅在受限目录用于恢复重放。
  完成后已不可恢复地删除受限目录内 11 个临时文件（145,882,653 bytes）并确认目录不存在；
  演练当时生产原库与原 key 未改写。
- 用户随后于 2026-08-26 单独授权本机生产迁移：`webui.db` 从 legacy 显式迁至
  `webui_0004`，恢复点为 `data/backups/webui-before-startup-20260826-225554.db`（SHA-256
  `3133e3db82f20699b7554d8916a065260e643e0224190e048f2ce9d6858302bc`）；`scheduler.db` 迁至
  `scheduler_0001`，恢复点为 `data/backups/scheduler-before-startup-20260826-225954.db`
  （SHA-256 `477f923f55f43f04ef4a527a4b154ba79ed400ebcda1e615a0b29d2b7fce6a79`）。两库
  `status` 均为 current，相邻 receipt 均已生成；恢复覆盖、旧备份处置和 key 轮换未授权。
- `start_all.bat` 已在本机接入 `scripts/check_dev_database_migrations.py`：服务启动前一次检查
  WebUI 与 Scheduler，任一落后就列全显式迁移命令并失败关闭，不再让监督器隐藏 Schema
  异常。历史迁移回归 49 passed，干净启动曾返回 0；真正 API 健康检查使用 `/api/health`，
  不把 `/health` 的 SPA HTML 200 当作后端健康证明。

### 5.2 依赖与供应链

- 以下扫描只证明 P0 当时的固定依赖/制品，不是当前持续零漏洞声明。
- Python runtime/collectors/dev/evaluation/gpu 五组已从空环境安装，`pip check`、深导入和
  `pip-audit==2.10.1` 通过；最终生产镜像实际 site-packages 为 0 个已知漏洞，GPU overlay
  为空，生产镜像不含评测/GPU 开发依赖。
- 前端 React Router 6.30.6 仍有 2 个 Moderate，当前 CSR/internal-route 接缝没有识别到公告
  可达路径；Promptfoo 0.122.1 隔离评测树仍有 5 个 High 传递节点，冻结六案例不进入相关 ZIP、
  图像或模型路径。两项均不进入无限期接受：Owner 最晚 2026-09-25 复查，触发条件变化立即停门。
- 公共证据只提交可复现摘要；本地 venv、漏洞 JSON、容器审计产物和 Promptfoo 结果不得入 Git。

### 5.3 工作树与提交安全

- 用户持有的 10 个 G1 freeze/fixture 修改和 `docker/phase4b/entrypoint.sh` 不属于本轮提交范围，
  禁止覆盖、清理或暂存。已确认可再生的 `.scratch/**`、`test-results/**` 与
  `frontend/premium-audit.json` 已按用户授权移入回收站。
- 只允许按审计过的文件 allowlist 逐文件暂存；禁止 `git add .`、`git add -A`、
  `git commit -a`、`git reset --hard` 和 `git clean`。
- #56～#58 已按同一可构建边界由 PR #76 合入；当前后续提交仍必须排除用户 G1 与本地产物。

## 6. 远端治理

- GitHub repository Ruleset
  [`main-required-ci-single-maintainer`](https://github.com/Eclipseic1848/Mangrove_ai/rules/21624053)
  （ID `21624053`）为 active，仅命中 `refs/heads/main`，`bypass_actors=[]` 且当前用户不可绕过。
- Ruleset 强制 PR、讨论解决、strict `backend-fast` / `frontend-build` / `secret-scan`（GitHub
  Actions Integration ID `15368`）和 `non_fast_forward`。仓库只有一个 collaborator，经用户
  明确确认采用单维护者模式：审批数为 0，`require_last_push_approval=false`。
- 临时 [PR #78](https://github.com/Eclipseic1848/Mangrove_ai/pull/78) 未合并：运行
  [33040744184](https://github.com/Eclipseic1848/Mangrove_ai/actions/runs/33040744184) 中
  `backend-fast` 失败且 PR 为 `BLOCKED`；修复后的运行
  [33040840714](https://github.com/Eclipseic1848/Mangrove_ai/actions/runs/33040840714) 三项全绿，
  PR 为 `CLEAN/MERGEABLE`。PR、远端/本地分支和临时 worktree 已安全清理，`main` 未含探针。
- #59 已关闭。未来实际加入第二位维护者时，应另开权限变更把审批数提升为 1 并重跑测试 PR；
  在此之前不得声称已有独立人工审批。

## 7. 下一路线与版本门

### P0 退出结果

1. #54～#58 均已关闭；#56～#58 由 PR #76 合入 `main@453e9008`，合并后 CI
   33039608828 三项全绿。#57 副本演练本身不等于生产迁移；后续生产迁移有单独授权和恢复点，
   但仍不等于 Secret/key 轮换。
2. #59 已按用户确认的单维护者模式完成 Ruleset 与不合并测试 PR 验收并关闭。
3. #60 已同步 `README.md`、`CONTRIBUTING.md`、`AGENTS.md`、`CONTEXT.md`、`SECURITY.md`、
   本台账和 `handoff.md`；`CODE_OF_CONDUCT.md`、MIT `LICENSE`、GitHub About 已检查且无需变化。
4. P0 退出只表示“可持续迭代的产品基线”，不自动产生 Tag、Release、部署或普通用户能力开放。

现场复核时远端无 Tag/Release；本地 `v0.0.4` 仅为历史语义。本轮没有版本号决定、Tag、Release
或部署；这些动作以后仍需重新现场核验和独立授权，不得从 P0 完成自动推断。

### P1 与 P2

R0–R6 是 P1 历史整改工作包，不是另造版本号；历史执行地图为 GitHub #113。旧方案终止后的未完成项见最新交接，不能直接据此恢复执行。

| 阶段 | 目标 | 当前状态 |
|---|---|---|
| P0 | 迁移、SecretRef、依赖、CI和发布治理基线 | 已收口，详见第4节 |
| P1-01 | 匿名网页首片与CoreMind Adapter工程闭环 | 已收口，不等于完整P1 |
| R0 | 安全、正确性、取消、分页、Owner、前端竞态、遥测 | #114–#123已关闭 |
| F0 | 统一旅程与交互原型 | #124已关闭；后续聊天体验纠正仍需落实 |
| R1 | 前置契约、只读边界、文件/图片/多轮/工具 | #125–#131、#133已关闭；#159模型配置已关闭 |
| R2 | 完整原件导出、自主公开搜索 | #132/#134已关闭；本机真实网络仍有资格限制 |
| R3 | 混合来源、复用、关联感知删除 | #135–#137已关闭 |
| R4 | 通用网站登录与HTTP/数据库只读来源 | #139/#186 已合入 `main`；#138 `CLOSED/NOT_PLANNED`，原扫码登录范围未完成 |
| R5 | 模板、记忆、自动任务、反馈、统一页面 | #140/#141/#142 已合入 `main` |
| R6 | 运维/多人、黄金任务、退役与交接 | 旧方案已终止；#143/#144/#145 `CLOSED/NOT_PLANNED`，部署、验收与退役未完成 |
| 人工全面审核 | 按真实产品旅程重新审核当前 `main` | 下一步；新会话先只读核验 |
| 新整改计划 | 根据人工审核结果重新拆票、排序和定义验收 | 等待用户给出 |
| P2 | 音视频；外部程序只读获取本人结果 | #146/#147 `OPEN`，仅为后期占位，不自动启动 |

版本推进门：当前公开开发基线不等于 P1 完成；旧整改已终止。人工审核与新整改计划完成后，
才重新判断 P1 功能候选和私有用户验收候选；完成实际部署、安全/恢复/真实用户门后才讨论稳定发布。
没有承诺发布日期或下一 semver。工程完成不自动授予
Tag、Release、部署或普通用户能力开放。P1顶层收口须检查README、Code of Conduct、Contributing、
MIT License、Security和GitHub About；无语义变化的文件记录“无需变化”。

历史 P1-02生命周期深化、P1-03可观测/多人安全、P1-04受控能力开放、P1-05前端质量目标由以上
整改闭环承接；多节点、对象存储、PostgreSQL、远程MCP/Registry和Linux/GPU部署只在需求与资源
具备时另行落实，不把技术设想当本期已承诺功能。外部业务写回、发布和投递不在产品范围。

## 8. 历史证据索引

### 当前 P1

- 当前整改：`docs/plans/2026-09-04-unified-data-lifecycle-remediation-plan.md`、ADR-0039。
- P1-01 规格：`docs/plans/2026-08-27-p1-01-anonymous-web-source-unified-workbench-spec.md`
- Runtime 决策：`docs/adr/0035-unified-data-workbench-and-coremind-runtime-adapter.md`
- 未上线迁移边界：`docs/adr/0036-single-workspace-with-verified-runtime-inheritance.md`
- CoreMind 0.7.1：`docs/plans/2026-08-27-p1-coremind-agentkernel-prototype-report.md`、PR #107。
- 工程收口：PR #110、CI run `33925622082`、已关闭 Issues #98/#83/#81。
- Agent-Reach：`docs/research/2026-08-31-agent-reach-mangrove-assessment.md`

### 历史 P0

- 路线图与 DoD：`docs/plans/2026-08-23-post-issues-productization-roadmap.md`
- P0-01/Candidate：`docs/plans/2026-08-23-p0-01-vnext-default-user-delivery-spec.md`、
  `docs/plans/2026-08-26-legacy-candidate-rebaseline-live-execution-report.md`
- #55：`docs/plans/2026-08-26-p0-04a-minimum-ci-implementation-report.md`
- #56：`docs/plans/2026-08-26-p0-05-explicit-database-migrations-spec.md`、
  `docs/plans/2026-08-26-p0-05-migration-tool-research.md`
- #57：`docs/plans/2026-08-26-p0-02-secretref-unification-spec.md`、
  `docs/plans/2026-08-26-p0-02-secretref-unification-implementation-report.md`
- #58：`docs/plans/2026-08-26-p0-03-dependency-security-evidence.md`
- #59：Ruleset `21624053`、PR #78、CI 33040744184 / 33040840714、已关闭 Issue #59。
- #60：本文件、根目录 `handoff.md`、公共入口文档和 GitHub Issue #60。

### 历史能力与不可变决策

- 领域与当前工程边界：`CONTEXT.md`、`AGENTS.md`
- Candidate 重验：`docs/adr/0033-candidate-reverification-and-verifier-ruleset.md`、
  `docs/adr/0034-historical-reverification-authority.md`
- vNext 默认切换：`docs/adr/0030-direct-vnext-default-cutover.md`、
  `docs/plans/2026-08-23-g3-vnext-cutover-execution-report.md`
- G1：`docs/plans/2026-08-20-g1-generalization-execution-report.md`
- G5 本机门：`docs/plans/2026-08-23-g5-local-prerequisite-execution-report.md`
- AC-07 总规格与生命周期：`docs/plans/2026-08-06-agentic-capability-ac07-spec.md`、
  `docs/adr/0029-capability-validation-lifecycle-and-platform-publication.md`

状态改变时先更新本文件，再同步精简 `handoff.md`。不要把滚动状态复制进 README、CONTEXT、ADR
或历史执行报告；文档自身未来 merge SHA 与 Issue 关闭状态必须从 GitHub 现场读取，不能用静态
占位符制造看似精确但一合并就过期的事实。
